# مقارنة الميزات: المرجع مقابل السورس — 2026-08-07

فحص لكل ميزة مذكورة في ملف المرجع (AI-Employee-Platform-Spec, Parts 1-17 + 13B) مقابل ما هو منفَّذ فعليًا في السورس كود بمجلد `c:\Users\INTEL\Desktop\Ai`. لا يتضمن هذا التقرير أي فحص هندسي أو جودة كود — مقارنة ميزات فقط، مبنية على فحص فعلي للملفات (لا افتراضات).

**الترقيم متسلسل عبر كل التقرير. كل ميزة مذكورة في المرجع لها إدخال، بدون استثناء.**

---
## GROUP 1-2: التأسيس والهوية ومساحة العمل والفريق (Parts 1-3)
---

# 1. المصادقة (Authentication)

## المطلوب حسب المرجع:
- Email + Password، تحقق بريد (OTP)، نسيت/إعادة تعيين كلمة المرور، Remember Me، جلسات آمنة، Supabase Auth كمزوّد هوية وحيد (Google OAuth كخيار أساسي، البريد كخيار ثانوي)، لا نظام مصادقة مخصص موازٍ.

## الموجود في السورس:
- `src/lib/supabase/{client,server}.ts`, `src/middleware.ts`, `src/features/auth/**`: Supabase Auth فعليًا هو مزوّد الهوية الوحيد. تسجيل دخول بالبريد/كلمة المرور، تحقق OTP رقمي (8 أرقام)، إعادة تعيين كلمة مرور عبر كود، Remember Me، جلسات httpOnly عبر `@supabase/ssr`. زر "Continue with Google" موجود في الكود.
- Rate limiting على تسجيل الدخول/OTP لكن بمفتاح البريد فقط (ليس IP).

## النتيجة:
❌ غير مكتملة.

النواقص:
- Google OAuth غير مُفعَّل فعليًا من طرف Supabase Dashboard (يحتاج إعداد خارجي — موثَّق في `DEFERRED_TASKS.md`).
- لا تسجيل لأحداث المصادقة (login/logout/فشل) في أي جدول تدقيق.
- التقييد بالمعدل (rate limit) بالبريد فقط وليس بالـ IP.
- لا اختبارات (`*.test.ts`) على `src/features/auth/` إطلاقًا.

---

# 2. تدفق التسجيل (Registration Flow)

## المطلوب حسب المرجع:
- إدخال بريد -> OTP -> تحقق -> كلمة مرور -> قبول الشروط -> إنشاء حساب -> إنشاء Workspace تلقائيًا -> بدء Onboarding. Google Sign-In متاح من الإطلاق كخيار أساسي.

## الموجود في السورس:
- التدفق موجود عبر Supabase Auth ويُنشئ Workspace تلقائيًا عند أول دخول (`profileSyncService`).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا خطوة/مربع اختيار صريح لـ"قبول الشروط" (Accept Terms).

---

# 3. الإعداد الأولي (Onboarding)

## المطلوب حسب المرجع:
- 10 خطوات: بيانات العمل، اسم وكيل الذكاء الاصطناعي، وصف العمل، اللغة، النبرة، الإبداع، ساعات العمل، التحويل للبشر، قاعدة المعرفة، ربط القنوات. أقل من 10 دقائق، حفظ تلقائي، قابلية التخطي، شريط تقدم.

## الموجود في السورس:
- `src/features/onboarding/**`: 9 خطوات فعلية تغطي محتوى الخطوات العشر، حفظ تلقائي حقيقي بعد كل خطوة (`bumpStep`)، خطوتا الوصف وقاعدة المعرفة قابلتان للتخطي.

## النتيجة:
❌ غير مكتملة.

النواقص:
- خطوة "ربط القنوات" أزرار معطّلة "Coming Soon" فقط (بلا تخطي صريح منفصل عن زر الإنهاء).
- لا شريط تقدم/قابلية تخطي موحّدة لكل الخطوات (فقط بعضها).

---

# 4. تعدد المستأجرين (Multi-Tenant)

## المطلوب حسب المرجع:
- كل شيء يتبع Workspace: المستخدمون، وكيل الذكاء الاصطناعي، القنوات، المنتجات، قاعدة المعرفة، جهات الاتصال، العملاء المحتملون، الطلبات، الملفات، الإعدادات، التحليلات، الذاكرة، المحادثات. لا شيء مشترك بين المستأجرين.

## الموجود في السورس:
- كل الجداول تقريبًا (`workspaces`, `users`, `ai-agents`, `channels`, `products`, `contacts`, `leads`, `orders`, `workspace-audit-logs`, `notifications`, `integrations`, `faqs`, `services`, `business-policies`, `ai-insights`) تحمل `workspaceId` مع cascade delete.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع (باستثناء عدم وجود جدول "Files" موحّد — انظر ميزة #7).

---

# 5. RBAC وإدارة الفريق (RBAC & Team Management)

## المطلوب حسب المرجع:
- 5 أدوار (Owner/Admin/Manager/Agent/Viewer) كبيانات وليست hardcoded. تسميات عرض مخصّصة قابلة للتوافق مع نوع العمل فوق نفس مستويات الصلاحية. دعوة بالبريد + دور، رمز دعوة آمن أحادي الاستخدام، عرض/تغيير/إلغاء العضوية، تسجيل كل إجراء في سجل التدقيق.

## الموجود في السورس:
- `src/db/schema/{roles,permissions,role-permissions,invitations}.ts`, `src/features/workspace/services/team.service.ts`: الأدوار الخمسة بيانات فعليًا، رمز دعوة SHA-256 أحادي الاستخدام صالح 7 أيام، تسجيل دعوة/تغيير دور/إزالة في `workspace_audit_logs`.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا حقل "تسمية عرض" مخصص منفصل عن `roles.name`/`key` — لا يوجد نظام تسميات صناعية قابلة للتخصيص فوق الأدوار الثابتة كما يطلب المرجع.
- لا تسجيل تدقيق لحدث "قبول الدعوة" نفسه (فقط الإرسال/الإلغاء/تغيير الدور/الإزالة).

---

# 6. تخزين الملفات (File Storage)

## المطلوب حسب المرجع:
- دعم صور، مستندات، صوت، فيديو، فواتير، تصديرات، ملفات معرفة، تضمينات مستقبلية — عبر تخزين كائنات وليس قاعدة البيانات مباشرة.

## الموجود في السورس:
- لا يوجد جدول `files`/`attachments` موحّد؛ فقط حقول URL نصية متفرقة (`workspaces.logoUrl`, `contacts.avatarUrl`). لا استخدام لـ Supabase Storage أو أي تخزين كائنات في أي مكان بالكود.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل شيء: كيان ملفات موحّد، رفع صور/مستندات/صوت/فيديو، تخزين الفواتير/التصديرات، ملفات المعرفة كملفات حقيقية (Cloudflare R2 غير مُزوَّد — موثَّق في `DEFERRED_TASKS.md`).

---

# 7. البحث الشامل (Search — من Part 2 وPart 3 معًا)

## المطلوب حسب المرجع:
- طبقة بحث تغطي: جهات الاتصال، المنتجات، الرسائل، الطلبات، قاعدة المعرفة، العملاء المحتملين — بحث عام واحد بالهاتف/الاسم/الرسالة/الطلب/المنتج/الوسم/الـ lead/الموعد.

## الموجود في السورس:
- بحث `ilike` منفصل موجود فقط داخل صفحات: جهات الاتصال (`contact.repository.ts`)، العملاء المحتملون (`lead.repository.ts`)، الطلبات (`order.repository.ts`).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا بحث في المنتجات، الرسائل، أو قاعدة المعرفة (FAQ/الخدمات/السياسات) إطلاقًا.
- لا صندوق بحث عام واحد (Global Search) يغطي كل الكيانات معًا — كل صفحة لها بحثها المعزول فقط.

---

# 8. القنوات (Channels)

## المطلوب حسب المرجع:
- الإصدار الأول: WhatsApp Business وInstagram DM فقط.

## الموجود في السورس:
- `src/db/schema/channels.ts`, `channel.repository.ts`: القناتان موجودتان كقيم enum وصفوف db بحالة `not_connected` فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تكامل فعلي وظيفي مع أي من واجهتي WhatsApp/Instagram (محجوب خارجيًا بانتظار حساب Meta Developer — موثَّق).

---

# 9. اتصال القنوات (Channel Connection)

## المطلوب حسب المرجع:
- "Connect WhatsApp" -> Meta OAuth -> اختيار العمل -> رقم الهاتف -> الصلاحيات -> نجاح. بلا مفاتيح يدوية.

## الموجود في السورس:
- `src/features/onboarding/components/channels-step.tsx`: أزرار معطّلة "Coming soon" فقط، بلا أي معالج نقر.

## النتيجة:
❌ غير مكتملة (غير موجودة إطلاقًا).

النواقص:
- كامل التدفق: Meta OAuth، اختيار العمل، رقم الهاتف، الصلاحيات، شاشة النجاح — لا شيء منها موجود.

---

# 10. إدارة القنوات (Channel Management)

## المطلوب حسب المرجع:
- إمكانية ربط أكثر من رقم WhatsApp/حساب Instagram، وتخزين: الحالة، صحة الاتصال، آخر مزامنة، الصلاحيات، حالة الـ Webhook.

## الموجود في السورس:
- `src/db/schema/channels.ts`: فقط حقلا `status` و`displayName`، مع قيد فريد يسمح بقناة واحدة فقط لكل نوع لكل Workspace.

## النتيجة:
❌ غير مكتملة.

النواقص:
- صحة الاتصال، آخر مزامنة، الصلاحيات، حالة الـ Webhook — لا شيء منها موجود كحقول.
- "رقم/حساب واحد أو أكثر" غير مدعوم (القيد الفريد يمنع أكثر من قناة واحدة لكل نوع).

---

# 11. نظام الـ Webhooks للقنوات (Webhook System)

## المطلوب حسب المرجع:
- كل قناة تستخدم Webhooks (لا polling مستمر). أحداث: رسالة جديدة، تسليم، قراءة، تفاعل، مرفق، إغلاق محادثة، تحديث الملف الشخصي، فقدان الاتصال.

## الموجود في السورس:
- لا يوجد أي route استقبال webhook لأي قناة (لا `/api/**/whatsapp`، لا `/api/**/instagram`، لا `/api/**/meta`). صندوق الوارد يعتمد على polling من طرف العميل (كل 4-5 ثوانٍ).

## النتيجة:
❌ غير مكتملة (غير موجودة إطلاقًا).

النواقص:
- كل الأحداث الثمانية المذكورة — لا شيء منها منفَّذ، لأن لا اتصال حي بأي قناة أصلًا.

---

# 12. صندوق الوارد الموحّد (Unified Inbox)

## المطلوب حسب المرجع:
- صندوق واحد يجمع WhatsApp وInstagram (ومستقبلًا Messenger/Telegram/Website Chat) في مكان واحد.

## الموجود في السورس:
- `src/features/inbox/components/inbox-list.tsx`: يعرض القنوات الثلاث (manual/whatsapp/instagram) في قائمة واحدة فعليًا.

## النتيجة:
❌ غير مكتملة.

النواقص:
- بما أن WhatsApp/Instagram غير متصلتين فعليًا، فإن قناة "manual" هي الوحيدة العاملة عمليًا اليوم.

---

# 13. ميزات صندوق الوارد (Inbox Features — فلاتر وبحث)

## المطلوب حسب المرجع:
- قائمة محادثات، بحث، فلاتر: غير مقروء، مُسنَد، مؤرشف، AI نشط، بشري نشط، يحتاج رد، أولوية، مثبّت، وسوم، آخر نشاط.

## الموجود في السورس:
- `inbox-list.tsx` يعرض القائمة فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا صندوق بحث ولا أي من الفلاتر العشرة المذكورة — ومعظمها ينقصه أصلًا حقل قاعدة بيانات مقابل (لا عمود `pinned`/`priority`/`archived` في جدول `conversations`).

---

# 14. عرض المحادثة (Conversation View)

## المطلوب حسب المرجع:
- يعرض: العميل، الصورة الرمزية، القناة، الحالة، الوسوم، الوكيل المُسنَد، حالة AI، الجدول الزمني، الرسائل، ملاحظات داخلية، ردود مقترحة، ملف العميل، الطلبات، المواعيد، حالة الـ lead، سجل النشاط.

## الموجود في السورس:
- `conversation-thread.tsx`: يعرض العميل، القناة، الحالة، حالة AI، الرسائل فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- الصورة الرمزية، الوسوم، عرض الوكيل المُسنَد (الحقل موجود بقاعدة البيانات لكن غير معروض)، الجدول الزمني، الملاحظات الداخلية، الردود المقترحة، ملف العميل المدمج، الطلبات، المواعيد، حالة الـ lead، سجل النشاط — لا شيء من هذا معروض داخل شاشة المحادثة نفسها (ملف العميل موجود كصفحة منفصلة فقط).

---

# 15. أنواع الرسائل (Message Types)

## المطلوب حسب المرجع:
- نص، صور، مستندات، صوت، فيديو (مستقبلي)، موقع (مستقبلي)، جهات اتصال (مستقبلي).

## الموجود في السورس:
- `src/db/schema/messages.ts`: عمود `content: text` واحد فقط، بلا حقول مرفقات/وسائط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- الصور، المستندات، الصوت — غير مدعومة إطلاقًا (فقط النص).

---

# 16. محرر الرسائل (Message Composer)

## المطلوب حسب المرجع:
- رد، إيموجي، مرفقات، قوالب، اقتراح AI، ملاحظة داخلية، تحويل للبشر، جدولة رسالة (مستقبلي).

## الموجود في السورس:
- `conversation-thread.tsx`: مربع نص + زر إرسال، وضع "تسجيل رسالة عميل"، إيقاف/استئناف AI، إغلاق المحادثة.

## النتيجة:
❌ غير مكتملة.

النواقص:
- إيموجي، مرفقات، قوالب، اقتراح AI (الذكاء الاصطناعي يرسل مباشرة وليس كاقتراح للموافقة)، ملاحظة داخلية حقيقية (وضع "تسجيل" يسجّل كلام العميل وليس ملاحظة موظف)، زر تحويل صريح للبشر (يوجد فقط إيقاف/استئناف AI)، جدولة رسالة.

---

# 17. الذكاء الاصطناعي داخل المحادثة (AI Inside Conversation)

## المطلوب حسب المرجع:
- يساعد الوكيل باستمرار: اقتراح رد، تلخيص، استخراج معلومات العميل، كشف النية، توصية منتجات، اقتراح الإجراء التالي، تحذير من عميل غاضب. لا يستبدل واجهة الوكيل، بل يساعده.

## الموجود في السورس:
- `ai.service.ts`: الذكاء الاصطناعي يولّد الرد كاملًا ويرسله تلقائيًا (وليس كاقتراح)، يلخّص تلقائيًا بعد التحويل، يستخرج/يحدّث معلومات العميل عبر tool calls.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا وضع "اقترح ردًا يوافق عليه الوكيل" — التصميم الحالي إما أن AI يرد تلقائيًا أو يتوقف كليًا، بخلاف مبدأ "يساعد الوكيل ولا يستبدله" في المرجع.
- لا تلخيص عند الطلب (يدويًا من الوكيل)، لا كشف نية صريح، لا توصية منتجات داخل الواجهة، لا اقتراح إجراء تالٍ، لا تحذير من عميل غاضب.

---

# 18. التحويل البشري داخل صندوق الوارد (Human Handover)

## المطلوب حسب المرجع:
- التحويل عند: طلب العميل صراحة، شكوى، استرجاع، مشكلة قانونية، نصيحة طبية، مشكلة دفع، نقاش حساس.

## الموجود في السورس:
- `request-human-handover.tool.ts`, `prompt-builder.ts`: مذكور صراحة: طلب صريح، شكوى، استرجاع، قانوني، طبي.

## النتيجة:
❌ غير مكتملة.

النواقص:
- "مشكلة دفع" و"نقاش حساس" غير مذكورين كمحفّزين صريحين.
- لا كشف صريح لـ"عميل غاضب" كمحفّز منفصل.

---

# 19. ملف العميل (Customer Profile)

## المطلوب حسب المرجع:
- الاسم، الهاتف، Instagram، الوسوم، الطلبات، المواعيد، نقاط الـ lead، الملاحظات، آخر تواصل، اللغة المفضلة، المنتجات المفضلة، ملخص AI، سجل المحادثات.

## الموجود في السورس:
- `src/app/dashboard/contacts/[contactId]/page.tsx`: الاسم، الهاتف، الوسوم، الطلبات، المواعيد، نقاط الـ lead، الملاحظات، آخر تواصل، ملخص AI، سجل زمني موحّد.

## النتيجة:
❌ غير مكتملة.

النواقص:
- Instagram (الحقل موجود بقاعدة البيانات `instagramId` لكن غير معروض).
- اللغة المفضلة (الحقل موجود `language` لكن غير معروض في هذه الصفحة).
- المنتجات المفضلة — لا حقل ولا واجهة إطلاقًا.

---

# 20. الإشعارات (Notifications)

## المطلوب حسب المرجع:
- رسالة جديدة، تحويل بشري، محادثة فائتة، فقدان اتصال، فشل webhook، إشارة (mention)، تعيين.

## الموجود في السورس:
- `src/db/schema/notifications.ts`: أنواع موجودة فعليًا: `subscription_expiring`, `subscription_suspended`, `automation`, `crm_followup`, `support_ticket_reply`.

## النتيجة:
❌ غير مكتملة.

النواقص:
- كل الأنواع الستة الخاصة بصندوق الوارد (رسالة جديدة، تحويل بشري، محادثة فائتة، فقدان اتصال، فشل webhook، إشارة، تعيين) — لا شيء منها موجود في نظام الإشعارات.

---

# 21. الوقت الحقيقي (Real-Time)

## المطلوب حسب المرجع:
- تحديث فوري بلا تحديث يدوي، واجهة متفائلة (Optimistic UI)، إعادة اتصال تلقائية.

## الموجود في السورس:
- `inbox-list.tsx`/`conversation-thread.tsx`: polling كل 4-5 ثوانٍ عبر `setInterval`.

## النتيجة:
❌ غير مكتملة (غير موجودة كما تصفها المواصفة).

النواقص:
- لا WebSocket/SSE أو أي دفع حقيقي (Push) — فقط استعلام دوري (Polling).
- لا واجهة متفائلة (التحديث يظهر فقط بعد استجابة الخادم).
- لا آلية إعادة اتصال (لا مفهوم "اتصال" أصلًا في نموذج الـ polling).

---
## GROUP 3: محرك الذكاء الاصطناعي (Part 4)
---

# 22. وكيل الذكاء الاصطناعي (AI Agent)

## المطلوب حسب المرجع:
- كل Workspace يملك وكيلًا واحدًا أو أكثر (MVP: وكيل واحد)، مع بنية جاهزة لدعم عدة وكلاء (مبيعات/دعم/حجز/تسويق) مستقبلًا بلا إعادة تصميم.

## الموجود في السورس:
- `src/db/schema/ai-agents.ts`, `src/features/ai/**`: وكيل واحد لكل Workspace، البنية (جدول منفصل + خدمات معزولة) تدعم التوسّع لاحقًا دون تغيير جوهري.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع (لـ MVP — تعدد الوكلاء نفسه مستقبلي وليس مطلوبًا الآن).

---

# 23. إعدادات وكيل الذكاء الاصطناعي (AI Agent Settings)

## المطلوب حسب المرجع:
- الاسم، الصورة الرمزية، اللغة، النبرة، الإبداع، وصف العمل، ساعات العمل، التحويل للبشر، قاعدة المعرفة، الأسئلة الشائعة، المنتجات، الخدمات — بلا أي مصطلح تقني.

## الموجود في السورس:
- `src/app/dashboard/ai-employee/**`: كل الحقول موجودة بلغة بسيطة (Friendly/Professional/... للنبرة، Low/Medium/High للإبداع)، تم التحقق من عدم تسرّب أي مصطلح تقني (Model/Provider/Token) في هذه الواجهة.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع.

---

# 24. ذاكرة الذكاء الاصطناعي (AI Memory — الطبقات الخمس)

## المطلوب حسب المرجع:
- ذاكرة ثابتة (هوية العمل)، ذاكرة معرفية (FAQ/منتجات/سياسات)، ذاكرة المحادثة الحالية، ذاكرة طويلة المدى (ملخصات وتفضيلات)، ذاكرة عمل مؤقتة (السياق الحالي).

## الموجود في السورس:
- ذاكرة العمل الحالية = آخر 30 رسالة (`AI_HISTORY_LIMIT`). الذاكرة طويلة المدى = `contacts.aiSummary` يُولَّد بعد كل تحويل بشري. المعرفة (وكيل+FAQ+منتجات+خدمات+سياسات) معزولة بالكامل حسب Workspace.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا "ذاكرة عمل مؤقتة" هيكلية أثناء المحادثة نفسها (فقط إعادة تشغيل الرسائل الخام)، لا استخراج وحفظ تفضيلات منظّمة أثناء المحادثة (فقط بعد الانتهاء/التحويل).

---

# 25. محرك الـ Prompt (Prompt Engine)

## المطلوب حسب المرجع:
- بناء ديناميكي معياري: System + Business + Knowledge + Conversation Summary + Recent Messages + Safety + Language + Tone -> Final Prompt. لا برومبت ضخم واحد.

## الموجود في السورس:
- `src/features/ai/prompt/prompt-builder.ts`: بناء فعلي من أقسام منفصلة قابلة لإعادة الاستخدام (الهوية، وصف العمل، ملخص العميل، اللغة، النبرة، الإبداع، الأسئلة الشائعة، المنتجات، الخدمات، السياسات، تعليمات منع الاختلاق، تعليمات منع الكشف/الحقن، تعليمات التحويل).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا حدّ أقصى لحجم الكتالوج المُدرَج (FAQ/منتجات/خدمات تُدرَج كاملة بلا سقف) — خطر تضخم تكلفة/tokens لعمل كبير الكتالوج.
- ساعات العمل محسوبة في مكان آخر بالنظام لكنها غير مُمرَّرة إلى هذا البرومبت.

---

# 26. محرك المعرفة (Knowledge Engine)

## المطلوب حسب المرجع:
- الرد يعتمد فقط على: معلومات العمل، FAQ، المنتجات، الخدمات، السياسات، الملفات المرفوعة. لا اختلاق. الاعتراف بعدم المعرفة وطرح أسئلة متابعة.

## الموجود في السورس:
- تعليمات صريحة في البرومبت بعدم الاختلاق والاعتراف بعدم المعرفة. أدوات إنشاء الطلب/الموعد تعيد التحقق من السعر/الاسم الحقيقي من الكتالوج بشكل مستقل عن ادعاء النموذج (دفاع مزدوج ضد الاختلاق).

## النتيجة:
❌ غير مكتملة.

النواقص:
- "الملفات المرفوعة" كمصدر معرفة غير موجودة (لا نظام رفع ملفات معرفة حقيقي — انظر ميزة #6).

---

# 27. توليد الردود (Response Generation)

## المطلوب حسب المرجع:
- كل رد يراعي: اللغة، النبرة، مرحلة المحادثة، نية العميل، قواعد العمل، ساعات العمل، حالة الـ lead، المعرفة، الأمان.

## الموجود في السورس:
- اللغة، النبرة، المعرفة، الأمان مُراعاة فعليًا في بناء البرومبت.

## النتيجة:
❌ غير مكتملة.

النواقص:
- "ساعات العمل" غير مُمرَّرة لتوليد الرد (نفس نقص #25).
- لا مراعاة صريحة/منظّمة لـ"مرحلة المحادثة" أو "حالة الـ lead" كمدخل مباشر لتوليد الرد.

---

# 28. كشف النية (Intent Detection)

## المطلوب حسب المرجع:
- كشف تلقائي: تحية، سؤال، شكوى، نية شراء، طلب دعم، حجز، إلغاء، استرجاع، حالة عاجلة، سبام، غير معروف.

## الموجود في السورس:
- لا وحدة/جدول مخصص لكشف النية — النية متروكة بالكامل لتقدير النموذج الضمني عند قراره استدعاء أداة (tool) أم لا.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة كخطوة منفصلة).

النواقص:
- كل الأنواع العشرة المذكورة — لا يوجد مخرج مُصنَّف/قابل للتدقيق لأيٍّ منها؛ الأمر ضمني بالكامل داخل حكم النموذج.

---

# 29. تأهيل العملاء المحتملين — جانب الذكاء الاصطناعي (Lead Qualification)

## المطلوب حسب المرجع:
- كشف تلقائي: مهتم، Hot/Warm/Cold Lead، عميل حالي، عميل عائد، VIP، مع تسجيل نقاط تلقائي.

## الموجود في السورس:
- `src/features/crm/lib/lead-score.ts`: تسجيل نقاط حتمي (deterministic) من إشارات هيكلية (عدد الرسائل، الطلبات، المواعيد، الوسوم، المرحلة، الحداثة) — موثَّق صراحة في الكود كـ"بديل حتمي عن تسجيل النقاط بالذكاء الاصطناعي".

## النتيجة:
❌ غير مكتملة.

النواقص:
- التسجيل حتمي (خوارزمية إحصائية) وليس مبنيًا على فهم AI لسياق المحادثة كما يصف المرجع حرفيًا ("AI calculates score").

---

# 30. استخراج الكيانات (Entity Extraction)

## المطلوب حسب المرجع:
- استخراج تلقائي: الاسم، الهاتف، البريد، العنوان، المنتج، الميزانية، تاريخ الموعد، تفاصيل الطلب، طريقة التواصل المفضلة — كبيانات منظّمة.

## الموجود في السورس:
- لا وحدة استخراج كيانات منفصلة؛ الاستخراج يحدث ضمنيًا فقط عندما يقرر النموذج استدعاء أداة مثل `update_contact_info`.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا مخرجات منظّمة/قابلة للتدقيق مستقلة عن قرار استدعاء أداة — لا استخراج ميزانية أو طريقة تواصل مفضلة في أي مكان بالكود.

---

# 31. محرك الأفعال / استدعاء الأدوات (Action Engine / Tool Calling)

## المطلوب حسب المرجع:
- AI -> Action Planner -> Tool Dispatcher -> Application Services -> Database. كل أداة: مخطط إدخال، تحقق، تفويض، سجل تدقيق، نتيجة.

## الموجود في السورس:
- `src/features/ai/tools/registry.ts`: نقطة تفويض واحدة حقيقية — تحقق Zod قبل التنفيذ، سياق (workspace/contact/conversation) يمنع الأداة من العمل خارج نطاقها، كل استدعاء (نجاح أو فشل) يُسجَّل في `ai_tool_executions`. حد أقصى 4 تكرارات لحلقة الأدوات.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع — أقوى جزء في هذا الفحص.

---

# 32. طبقة الأمان (Safety Layer)

## المطلوب حسب المرجع:
- منع الاختلاق، مخالفة السياسات، النصائح غير الآمنة، الوعود الكاذبة، تسريب البيانات الحساسة، تسريب بين المستأجرين. عدم كشف البرومبت الداخلي أو معلومات المزوّد أبدًا.

## الموجود في السورس:
- تعليمات صريحة لمنع الكشف عن التعليمات/المزوّد ومنع حقن البرومبت (`prompt-builder.ts`). عزل كامل بين المستأجرين تم التحقق منه.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا طبقة تنقية ثانية (Post-generation filter) لمخرجات النموذج — الحماية كلها على مستوى التعليمات (prompt-only)، بلا فحص لاحق للنص الناتج.

---

# 33. موجه الذكاء الاصطناعي (AI Router)

## المطلوب حسب المرجع:
- اختيار المزوّد، اختيار النموذج، fallback تلقائي، إعادة محاولة، تحديد إن كان يحتاج سياقًا أكبر، تحديد إن كان نموذج محلي كافٍ أم يحتاج نموذجًا متميزًا — كل ذلك تلقائيًا.

## الموجود في السورس:
- `src/features/ai/router/ai-router.ts`: 15 سطرًا، يُرجع دائمًا نفس مزوّد Claude بلا شرط. التعليق في الكود نفسه يقر بأنه "مزوّد واحد اليوم... هذا هو الموضع الذي سيُبنى فيه التوجيه المستقبلي".

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة — عنصر نائب فقط).

النواقص:
- كل شيء: اختيار المزوّد، اختيار النموذج، fallback، إعادة محاولة، تحسين السياق/التكلفة/الجودة.

---

# 34. استراتيجية المزودين (Provider Strategy)

## المطلوب حسب المرجع:
- مزوّدون مدعومون: Claude, OpenAI, Gemini, OpenRouter, Ollama — كل واحد ينفّذ نفس الواجهة تمامًا.

## الموجود في السورس:
- `src/features/ai/providers/claude.provider.ts`: مزوّد Claude فقط منفَّذ خلف واجهة `AIProvider` (البنية تسمح بإضافة مزوّدين آخرين دون كسر شيء، لكن لم يُبنَ أي منهم).

## النتيجة:
❌ غير مكتملة.

النواقص:
- OpenAI, Gemini, OpenRouter, Ollama — لا شيء منها منفَّذ (Claude فقط).

---

# 35. الوضع الذكي (Smart Mode)

## المطلوب حسب المرجع:
- تحسين تلقائي للسرعة، الجودة، التكلفة، الموثوقية، فهم اللغة، تعقيد المحادثة — بلا تدخل المستخدم.

## الموجود في السورس:
- لا منطق تحسين تلقائي من أي نوع؛ يعتمد كليًا على استدعاء واحد لمزوّد واحد بإعدادات ثابتة.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل عناصر التحسين التلقائي المذكورة — غير موجودة، لأن الموجه (Router) نفسه عنصر نائب (انظر #33).

---

# 36. المراقبة (AI Observability)

## المطلوب حسب المرجع:
- تسجيل: بناء البرومبت، استرجاع المعرفة، اختيار المزوّد، استخدام fallback، توليد الرد، تنفيذ الأداة، التصعيد، الفشل، إعادة المحاولة، الكمون، التكلفة.

## الموجود في السورس:
- `src/db/schema/ai-usage.ts`, `src/features/ai/tools/registry.ts` (`ai_tool_executions`): تسجيل فعلي لكل استدعاء AI (نجاح/فشل/كمون/tokens) ولكل تنفيذ أداة.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تسجيل منفصل لخطوة "بناء البرومبت" أو "استرجاع المعرفة" كأحداث مستقلة؛ لا "استخدام fallback" لأن لا fallback أصلًا؛ لا تتبع للتكلفة الفعلية (بالدولار) — مُستبعد عمدًا.

---

# 37. إدارة تكلفة الذكاء الاصطناعي (AI Cost Management)

## المطلوب حسب المرجع:
- معرفة: التكلفة اليومية/الشهرية، لكل Workspace/مزوّد/نموذج/محادثة/عميل، اتجاه التكلفة، توقع مستقبلي. (مذكورة في Part 4 وPart 8 معًا).

## الموجود في السورس:
- `src/features/platform-admin/repository/ai-usage-admin.repository.ts`: تجميع لعدد الطلبات ومجموع الـ tokens ومتوسط الكمون فقط، لكل Workspace/نموذج.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا حساب تكلفة بالدولار في أي مكان بالكود (قرار مقصود موثَّق في تعليق الكود: "تقدير التكلفة بالدولار مُستبعد عمدًا"). لا اتجاه تكلفة، لا توقع مستقبلي.

---
## GROUP 4: CRM ومحرك المبيعات (Part 5)
---

# 38. دورة حياة العميل (Customer Lifecycle)

## المطلوب حسب المرجع:
- زائر مجهول -> محادثة بدأت -> lead -> مؤهَّل -> مهتم -> موعد (اختياري) -> عرض سعر (اختياري) -> طلب -> عميل -> عميل متكرر -> VIP -> عميل مخلص. تحديث تلقائي للمراحل.

## الموجود في السورس:
- `src/db/schema/contacts.ts` (`lifecycleStage`), `src/features/crm/services/crm.service.ts` (`advanceLifecycleStage`): تقدّم تلقائي للمرحلة عند تحقق أحداث إيراد (تسليم/إكمال طلب).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا مرحلة "عرض سعر" (Quotation) منفصلة، لا تمييز صريح بين "عميل متكرر" و"VIP" و"عميل مخلص" كمراحل مستقلة (فقط مراحل مبسّطة).

---

# 39. جهات الاتصال (Contacts)

## المطلوب حسب المرجع:
- الاسم، الهاتف، WhatsApp، Instagram، البريد، الصورة، الدولة، المدينة، اللغة، المنطقة الزمنية، تاريخ الميلاد، الجنس، نوع العميل، المصدر، نقاط الـ lead، الحالة، الوكيل المُسنَد، تاريخ الإنشاء، آخر نشاط، الوسوم، الملاحظات، ملخص AI، عدد المحادثات، عدد الطلبات، عدد المواعيد، القيمة الدائمة (LTV)، اللغة المفضلة، المنتجات المفضلة، تفضيلات التواصل.

## الموجود في السورس:
- `src/db/schema/contacts.ts`: الاسم، الهاتف، WhatsApp، Instagram، البريد، الصورة، الدولة، المدينة، اللغة، الوسوم، الملاحظات، ملخص AI، المصدر، الحالة/المرحلة، الوكيل المُسنَد، آخر تواصل.

## النتيجة:
❌ غير مكتملة.

النواقص:
- المنطقة الزمنية، تاريخ الميلاد، الجنس، القيمة الدائمة (LTV) محسوبة ومخزَّنة، المنتجات المفضلة، تفضيلات التواصل — لا شيء من هذا موجود كحقل.
- عدد المحادثات/الطلبات/المواعيد يُحسَب عند العرض وليس مخزَّنًا كعداد.

---

# 40. العملاء المحتملون — Leads

## المطلوب حسب المرجع:
- إنشاء تلقائي من كشف نية الشراء/طلب خدمة/عرض سعر/حجز موعد/استفسار منتج/حالة دعم — لا إنشاء يدوي كمسار أساسي.

## الموجود في السورس:
- `src/features/crm/services/crm.service.ts` (`createLeadFromConversation`): إنشاء تلقائي من المحادثة، متعادل الفعل (idempotent) — لا يكرر إنشاء lead لنفس المحادثة.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع.

---

# 41. تسجيل نقاط العملاء المحتملين (Lead Scoring)

## المطلوب حسب المرجع:
- عوامل: طول المحادثة، عدد الأسئلة، نية الشراء، الميزانية المذكورة، الإلحاح، مشتريات سابقة، التفاعل، زمن الاستجابة، قواعد العمل. نتيجة 0-100 (Cold/Warm/Hot/Priority).

## الموجود في السورس:
- `src/features/crm/lib/lead-score.ts`: نتيجة 0-100 محسوبة ومقيّدة (clamped) من: عدد الرسائل، الطلبات، المواعيد، الوسوم، المرحلة، الحداثة — مختبرة جيدًا (`lead-score.test.ts`).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا اعتماد على "الميزانية المذكورة" أو "عدد الأسئلة" أو "زمن الاستجابة" كعوامل — النموذج حتمي مبني على بيانات هيكلية بديلة، وليس تحليل محتوى المحادثة كما يصف المرجع.

---

# 42. خط الأنابيب (Pipeline)

## المطلوب حسب المرجع:
- مراحل: New Lead, Qualified, Contacted, Negotiation, Waiting, Appointment, Proposal Sent, Won, Lost, Cancelled.

## الموجود في السورس:
- `src/db/schema/leads.ts`: تعداد المراحل يطابق المرجع تمامًا (`lead-board.tsx` يعرضها ككانبان).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا حراسة انتقال حالة (state machine) — يمكن نقل أي بطاقة من أي مرحلة لأي مرحلة أخرى بلا قيود منطقية (مثلًا Won -> New مقبول تقنيًا).

---

# 43. أتمتة خط الأنابيب (Pipeline Automation)

## المطلوب حسب المرجع:
- نقل بطاقة يُطلق أتمتة تلقائيًا (مثال: Won -> إنشاء عميل -> إنشاء طلب -> إشعار الفريق -> بدء متابعة).

## الموجود في السورس:
- `crm.service.ts`: نقل مرحلة يستدعي `automationService.dispatch(..., "lead_stage_changed")` فعليًا — يمكن بناء أي سير عمل فوق هذا الحدث.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع (الحدث موجود ويعمل؛ أي سيناريو أتمتة فوقه هو مسؤولية إعداد المستخدم لسير العمل، لا نقص برمجي).

---

# 44. الجدول الزمني للعميل (Customer Timeline)

## المطلوب حسب المرجع:
- خط زمني واحد يجمع: المحادثة، الرسائل، الطلبات، المواعيد، الملاحظات، الوسوم، المدفوعات، الفواتير، التعيينات، أفعال AI، أفعال بشرية، البريد، مكالمات مستقبلية.

## الموجود في السورس:
- `src/features/crm/lib/timeline.ts`: يجمع المحادثات، العملاء المحتملين، الطلبات، المواعيد، الأنشطة في استعلام واحد (`Promise.all`، لا N+1)، مرتّبة زمنيًا.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا مدفوعات ولا فواتير (لا نظام فوترة حقيقي أصلًا — انظر #97-100).
- لا Pagination على الجدول الزمني الطويل.

---

# 45. كتالوج المنتجات (Product Catalog)

## المطلوب حسب المرجع:
- منتجات، خدمات، فئات، متغيّرات (variants)، صور، سعر، خصم، توفر، مخزون (مستقبلي)، وصف، وسوم، حالة، رؤية للـ AI.

## الموجود في السورس:
- `src/db/schema/products.ts`: الاسم، الوصف، السعر، صورة واحدة، `isActive`، معزول حسب Workspace.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا متغيّرات (variants)، لا معرض صور متعدد، لا خصم، لا فئات (Categories)، لا علم "رؤية AI" منفصل عن `isActive` (نفس الحقل يخلط بين "ظاهر للعميل" و"قابل للاستخدام من AI").

---

# 46. الخدمات (Services)

## المطلوب حسب المرجع:
- كل خدمة: المدة، السعر، التوفر، الوصف، الموظف المُسنَد (مستقبلي).

## الموجود في السورس:
- `src/db/schema/services.ts`: الاسم، الوصف، السعر، `isActive`.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا حقل "المدة" (Duration) — وهو أساسي لحجز المواعيد لاحقًا، لا حقل توفر منفصل.

---

# 47. الطلبات (Orders)

## المطلوب حسب المرجع:
- حالات: Draft…Refunded (9 حالات). يخزّن: العميل، العناصر، الكمية، السعر، الخصم، الضريبة، التوصيل، طريقة الدفع، الحالة، الجدول الزمني، الملاحظات، المرفقات.

## الموجود في السورس:
- `src/db/schema/orders.ts`: تعداد الحالات التسع يطابق المرجع تمامًا. العناصر مع الاسم/السعر/الكمية (مُلتقَطة وقت الطلب).

## النتيجة:
❌ غير مكتملة.

النواقص:
- **لا خصم، لا ضريبة، لا طريقة توصيل، لا طريقة دفع إطلاقًا** — غائبة تمامًا من الـ schema والتحقق (`order-total.ts` مجرد `unitPrice × quantity`).
- لا مرفقات.
- لا حراسة انتقال حالة (أي انتقال حالة مقبول تقنيًا، مثل Cancelled -> Confirmed).

---

# 48. المواعيد (Appointments)

## المطلوب حسب المرجع:
- العميل، الخدمة، التاريخ، الوقت، المدة، الحالة، الموظف المُسنَد، التذكير، الملاحظات، مزامنة تقويم مستقبلية.

## الموجود في السورس:
- `src/db/schema/appointments.ts`: العميل، الخدمة (مُلتقَطة اسميًا)، التاريخ/الوقت، المدة، الحالة، الملاحظات.

## النتيجة:
❌ غير مكتملة.

النواقص:
- **لا حقل "موظف مُسنَد" إطلاقًا.**
- **لا آلية تذكير من أي نوع** (لا بريد، لا إشعار، لا cron مخصص لهذا الغرض).
- لا حراسة انتقال حالة.

---

# 49. المهام (Tasks)

## المطلوب حسب المرجع:
- إنشاء مهام داخلية، تعيين للفريق، تاريخ استحقاق، أولوية، تذكير، حالة، مرفقات، مهام مقترحة من AI.

## الموجود في السورس:
- `src/db/schema/tasks.ts`, `task.service.ts`: العنوان، تاريخ الاستحقاق، الأولوية، الحالة (`open`/`done`)، تعيين لمستخدم، ربط بجهة اتصال، تسجيل نشاط عند الإنشاء/الإكمال.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تذكير، لا مرفقات، لا مهام مُقترَحة تلقائيًا من AI.

---

# 50. الملاحظات (Notes)

## المطلوب حسب المرجع:
- ملاحظات خاصة، ملاحظات فريق، ملاحظات AI، ملاحظات مثبَّتة.

## الموجود في السورس:
- `src/db/schema/notes.ts`, `note.service.ts`: محتوى، تثبيت (`pinned`)، مؤلف، ربط بجهة اتصال.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تمييز بين "ملاحظة خاصة" و"ملاحظة فريق"، لا نوع "ملاحظة AI" منفصل (كل الملاحظات من نوع واحد بشري).

---

# 51. الوسوم (Tags)

## المطلوب حسب المرجع:
- مرنة: VIP، مهتم، عائد، شكوى، جملة، تجزئة، عاجل، دفع معلّق، يدوي، تلقائي، مولَّد بالـ AI.

## الموجود في السورس:
- `contacts.tags` (jsonb array) قابل للإضافة/الحذف بحرية من أي مصدر (يدويًا أو عبر أداة AI `add_tag`).

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع (المرونة الأساسية موجودة؛ التصنيفات المذكورة أمثلة توضيحية وليست قائمة مُقفَلة).

---

# 52. الذكاء الاصطناعي داخل CRM (AI Inside CRM)

## المطلوب حسب المرجع:
- توليد ملخصات، اقتراح الإجراء التالي، توقع احتمال الشراء، كشف مخاطر فقدان العميل، توصية منتجات، كشف المشاعر، كشف الإلحاح، تحديث نقاط الـ lead، توليد متابعة، اقتراح مواعيد — كل ذلك في الخلفية.

## الموجود في السورس:
- ملخصات AI بعد التحويل، تحديث نقاط الـ lead (حتمي)، مخاطر فقدان العميل موجودة لكن في وحدة Campaigns وليس CRM نفسها.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا اقتراح إجراء تالٍ، لا توقع احتمال شراء، لا كشف مشاعر، لا كشف إلحاح، لا توصية منتجات آلية، لا اقتراح مواعيد تلقائي داخل CRM.

---

# 53. محرك المتابعة (Follow-up Engine)

## المطلوب حسب المرجع:
- عميل خامل 7 أيام -> اقتراح متابعة؛ موعد غدًا -> تذكير؛ عرض سعر معلّق -> إشعار المبيعات؛ طلب غير مكتمل -> تذكير.

## الموجود في السورس:
- `src/app/api/cron/crm-followups/route.ts`: يوجد cron لمتابعة العملاء الخاملين (`findStaleOpenLeads`).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تذكير مواعيد (مرتبط بنقص #48)، لا إشعار "عرض سعر معلّق" (لا مفهوم عرض سعر أصلًا)، لا تذكير طلب غير مكتمل.

---

# 54. سجل الأنشطة (Activity Log)

## المطلوب حسب المرجع:
- تتبّع: إنشاء lead، تحديث طلب، حجز موعد، تعيين محادثة، إضافة وسم، رد AI، استلام بشري، تغيير إعدادات، تسجيل دخول، تحديث صلاحيات.

## الموجود في السورس:
- `src/features/crm/repository/activity.repository.ts`: يسجّل فعليًا معظم أحداث CRM (lead/order/appointment/note/task) مع نسب الفاعل (AI أو بشري).

## النتيجة:
❌ غير مكتملة.

النواقص:
- تسجيل الدخول وتحديث الصلاحيات يُسجَّلان في سجل تدقيق منفصل (`workspace_audit_logs`) وليس في نفس Activity Log الخاص بالعميل — لا تجميع موحّد للنوعين.

---

# 55. أحداث الأتمتة (Automation Ready — CRM Events)

## المطلوب حسب المرجع:
- كل حدث CRM يجب أن يُصدر حدثًا داخليًا يُستخدم لاحقًا في الأتمتة: LeadCreated، OrderCompleted، AppointmentBooked، ConversationAssigned، CustomerTagged.

## الموجود في السورس:
- `crm.service.ts`, `order.service.ts`, `appointment.service.ts`: `lead_created`, `lead_stage_changed`, `order_created`, `order_status_changed`, `appointment_created`, `appointment_status_changed` تُطلَق فعليًا ومتزامنة مع كل تعديل.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا حدث "ConversationAssigned" مستقل، لا حدث "CustomerTagged" من إضافة وسم يدوية (الأتمتة تستمع فقط لوسم أضافه AI عبر أداته الخاصة، وليس كل إضافة وسم — قرار مقصود موثَّق لتفادي حلقات تافهة).

---
## GROUP 5: محرك الأتمتة والمراسلة (Part 6)
---

# 56. محرك الأتمتة (بنية عامة)

## المطلوب حسب المرجع:
- Trigger -> Conditions -> Actions -> Completion. حالات: Draft, Active, Paused, Disabled, Archived.

## الموجود في السورس:
- `src/db/schema/workflows.ts`, `automation.service.ts`: البنية موجودة فعليًا بنفس التسلسل والحالات الخمس.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع.

---

# 57. المشغلات (Triggers)

## المطلوب حسب المرجع:
- محادثة بدأت، رسالة استُلمت/رُدَّ عليها، lead أُنشئ/حُدِّث/فاز/خسر، طلب أُنشئ/دُفع/سُلِّم، موعد أُنشئ/أُلغي، جهة اتصال أُنشئت، وسم أُضيف، عميل أصبح VIP، تحويل بشري، فشل AI، قناة اتصلت، Workspace أُنشئ، اشتراك جُدِّد، دفع فشل، مستخدم دُعي.

## الموجود في السورس:
- 11 نوع محفّز فعليًا: lead/order/appointment/tag/message/handover/ai_failed.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا محفّز منفصل لـ"Lead Won"/"Lead Lost" (مدموج في `lead_stage_changed` عام)، لا "Order Paid" منفصل (مدموج في `order_status_changed` عام)، لا محفّز لقناة اتصلت، Workspace أُنشئ، اشتراك جُدِّد، دفع فشل، أو مستخدم دُعي (قرارات مقصودة موثَّقة — لا تتوافق مع نموذج الحدث لكل جهة اتصال في هذا المحرك).

---

# 58. الشروط (Conditions)

## المطلوب حسب المرجع:
- AND/OR، مجموعات متداخلة، عوامل مقارنة (نقاط lead، اللغة، وسم، قيمة طلب، شراء سابق، ساعات العمل، الوكيل، القناة، المشاعر، ثقة AI، نوع العميل، الموقع).

## الموجود في السورس:
- `automation.service.ts` (`evaluateConditions`): قائمة مسطّحة واحدة بـ AND/OR على المستوى الأعلى فقط، حقول: وسم، لغة، نقاط lead، قيمة طلب، ساعات العمل.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا مجموعات متداخلة (nested groups) — موثَّق كتبسيط مقصود.
- لا شرط على: شراء سابق، الوكيل، القناة، المشاعر، ثقة AI، نوع العميل، الموقع.

---

# 59. الأفعال (Actions)

## المطلوب حسب المرجع:
- إرسال رد AI، تعيين وكيل، إنشاء lead/طلب، حجز موعد، تحديث جهة اتصال، إضافة/إزالة وسم، إنشاء مهمة/ملاحظة، إشعار الفريق، إرسال بريد/قالب WhatsApp، أرشفة/إغلاق محادثة، تشغيل سير عمل آخر، webhook/API، (مستقبلي: SMS/مكالمة صوتية).

## الموجود في السورس:
- 15 نوع فعل فعليًا: إضافة/إزالة وسم، إشعار المالك، إنشاء مهمة/ملاحظة، تحديث جهة اتصال، إنشاء lead/طلب، حجز موعد، تعيين وكيل، webhook، تشغيل سير عمل آخر، إرسال رد AI، طلب موافقة.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا فعل "إرسال بريد/WhatsApp مباشر لجهة اتصال" كفعل مستقل (فقط "إشعار المالك" بالبريد).
- لا SMS أو مكالمة صوتية (مستقبلي أصلًا في المرجع).

---

# 60. التأخير (Delay)

## المطلوب حسب المرجع:
- انتظار دقائق/ساعات/أيام/حتى تاريخ محدد/حتى ساعات العمل/حتى فتح العمل.

## الموجود في السورس:
- `workflow_pending_runs` table: تأخير مثابر (يبقى بعد إعادة تشغيل الخادم) — لكن بوحدة الأيام فقط (`delayDays: integer`)، يُسحَب عبر cron يومي.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا انتظار بالدقائق أو الساعات (أيام فقط)، لا "حتى تاريخ محدد"، لا "حتى ساعات العمل/فتح العمل" كخيار تأخير. الكمون الفعلي حتى 24 ساعة بسبب السحب اليومي.

---

# 61. الموافقة البشرية (Human Approval)

## المطلوب حسب المرجع:
- إيقاف التنفيذ، إنشاء مهمة موافقة، استئناف عند الموافقة أو إنهاء/تفريع عند الرفض، إشعار المعتمِد.

## الموجود في السورس:
- `workflow_approvals` table, `decideApproval`: عند الطلب يُنشأ صف موافقة، وعند القبول يُشغَّل سير عمل ثانٍ منفصل (`onApproveWorkflowId`).

## النتيجة:
❌ غير مكتملة.

النواقص:
- ليس إيقاف/استئناف حقيقي لنفس السير — بل تشغيل سير عمل منفصل بديل عند الموافقة (بديل عملي موثَّق، وليس ما يصفه المرجع حرفيًا). الرفض لا يُفرِّع لأي إجراء، فقط يُسجَّل القرار.

---

# 62. مولد سير العمل بالذكاء الاصطناعي (AI Workflow Generator)

## المطلوب حسب المرجع:
- وصف بلغة طبيعية -> AI يحوّله لنفس مخطط Workflow JSON (Trigger/Conditions/Actions) -> يملأ الـ canvas تلقائيًا للمراجعة.

## الموجود في السورس:
- `generate-workflow.action.ts`, `aiService.generateWorkflowFromDescription`: يعمل فعليًا، يُنتج نفس بنية سير العمل اليدوي، محمي بصلاحية وعلم ميزة (feature flag)، مُسجَّل في التدقيق.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع.

---

# 63. الإرسال متعدد القنوات (Multi-Channel Dispatch)

## المطلوب حسب المرجع:
- أفعال إضافية: إرسال WhatsApp، إرسال SMS، إرسال بريد — كل واحد عبر قناة/بيانات اعتماد Workspace الخاصة، مع متغيّرات ديناميكية.

## الموجود في السورس:
- إرسال بريد فقط (`notify_owner_email`) عبر `EmailService` الخاص بالمنصة (وليس بيانات اعتماد Workspace الخاصة).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا إرسال WhatsApp (القناة نفسها غير متصلة — انظر #9)، لا إرسال SMS، ولا مفهوم "قناة/بيانات اعتماد Workspace الخاصة" لإرسال البريد (يُرسَل من مُرسِل واحد على مستوى المنصة).

---

# 64. محرك التنفيذ (Execution Engine)

## المطلوب حسب المرجع:
- تنفيذ غير متزامن، لا يحجب طلب المستخدم أبدًا، طابور، إعادة محاولة، مهلة زمنية، Dead Letter Queue، سجل تنفيذ.

## الموجود في السورس:
- `automation.service.ts`: سجل تنفيذ حقيقي (`workflow_executions`)، إعادة محاولة واحدة (250ms)، منع حلقات لا نهائية (`MAX_WORKFLOW_CHAIN_DEPTH = 5`) بشكل ممتاز.

## النتيجة:
❌ غير مكتملة.

النواقص:
- **التنفيذ متزامن (`await`) داخل نفس طلب المستخدم مباشرة** (وليس عبر طابور Redis/BullMQ رغم توفر Redis) — يخالف المتطلب الصريح "لا يحجب الطلب أبدًا".
- لا Dead Letter Queue حقيقي (فشل التنفيذ = صف واحد `success:false`، بلا آلية إعادة إرسال).
- لا "claiming" لمنع تنفيذ مزدوج لنفس صف التأخير عند تداخل استدعاءات الـ cron.

---

# 65. قوالب سير العمل (Workflow Templates)

## المطلوب حسب المرجع:
- رسالة ترحيب، متابعة lead جديد، تذكير موعد، موعد فائت، محادثة مهجورة، طلب مهجور، رضا العميل، شكر، عميل VIP، تصعيد شكوى، رد إغلاق العمل، خارج أوقات الدوام.

## الموجود في السورس:
- لا مكتبة قوالب جاهزة معروضة للمستخدم — كل سير عمل يُبنى من الصفر (يدويًا أو بمولد AI).

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل القوالب الاثني عشر المذكورة — لا شيء منها موجود كقالب جاهز للاستخدام الفوري.

---

# 66. باني سير العمل المرئي (Workflow Builder)

## المطلوب حسب المرجع:
- سحب وإفلات، تكبير، تراجع/إعادة، تكرار، قوالب، بحث عقد، خريطة مصغّرة، تحقق.

## الموجود في السورس:
- `src/features/automation/components/workflow-canvas/**` (React Flow): عرض مرئي لثلاث عقد ثابتة (Trigger/Conditions/Action) قابلة للتحرير، مع تكبير/تصغير وخريطة مصغّرة.

## النتيجة:
❌ غير مكتملة.

النواقص:
- ليس سحب وإفلات حقيقي متعدد العقد (3 عقد ثابتة فقط تمثّل شكل المحرك الخطي، وليس محرر رسم بياني عام)، لا تراجع/إعادة، لا تكرار، لا بحث عقد، لا قوالب داخل الباني نفسه.

---

# 67. الصلاحيات على الأتمتة (Permissions)

## المطلوب حسب المرجع:
- صلاحيات: Owner, Admin, Manager, Read Only.

## الموجود في السورس:
- كل إجراءات إدارة سير العمل (إنشاء/حذف/تفعيل/توليد AI/قرار موافقة) تفرض `automation.workflows.manage` فعليًا — تم التحقق مباشرة من كل ملف action. كانت هذه ثغرة معروفة سابقًا وتم إغلاقها بالكامل.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع.

---

# 68. سجلات سير العمل (Workflow Logs)

## المطلوب حسب المرجع:
- كل تنفيذ يسجّل: سير العمل، المحفّز، وقت/مدة التنفيذ، النتيجة، الخطوة الفاشلة، عدد المحاولات، المتغيّرات، استدعاءات AI.

## الموجود في السورس:
- `workflow_executions` table: يسجّل النتيجة وعدد المحاولات والخطوة.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تسجيل صريح لـ"المتغيّرات" المستخدمة في كل تنفيذ، لا ربط مباشر بعدد استدعاءات AI لكل تنفيذ سير عمل محدد.

---
## GROUP 6: العمليات التجارية (Parts 7-9)
---

# 69. لوحات تحكم حسب الدور (Role-Based Dashboards)

## المطلوب حسب المرجع:
- Owner, Admin, Manager, Agent — كل دور يرى معلومات مختلفة ومناسبة له.

## الموجود في السورس:
- `dashboard.service.ts`: نمطان فقط — "مساهم فردي" (Agent/Viewer) مقابل البقية (Owner/Admin/Manager يرون نفس اللوحة تمامًا).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تمايز فعلي بين لوحة Owner ولوحة Admin ولوحة Manager — الثلاثة متطابقة.

---

# 70. لوحة تحكم المالك (Owner Dashboard)

## المطلوب حسب المرجع:
- محادثات اليوم، leads جديدة، Hot Leads، مواعيد اليوم، طلبات معلّقة/مكتملة، الإيراد المقدَّر/الشهري، معدل التحويل، زمن الاستجابة، محادثات AI/بشرية نشطة، محادثات فائتة، رضا العميل، حالة الاشتراك، استخدام AI، أفضل قناة/منتج/خدمة/وكيل، النشاط الأخير، مؤشر صحة العمل.

## الموجود في السورس:
- `dashboard.service.ts`: محادثات اليوم، leads جديدة، مواعيد اليوم، طلبات اليوم، إيراد اليوم، زمن استجابة مدمج، مؤشر صحة العمل، احتياج انتباه (متأخرين/تحويل بشري).

## النتيجة:
❌ غير مكتملة.

النواقص:
- Hot Leads كبطاقة عدّاد منفصلة، الطلبات المعلّقة/المكتملة كتقسيم منفصل، الإيراد الشهري (منفصل عن اليومي)، معدل التحويل كإحصائية ظاهرة، محادثات AI/بشرية النشطة (محسوبة داخليًا لكن غير معروضة)، رضا العميل (لا يوجد CSAT في النظام كله)، حالة الاشتراك على اللوحة الرئيسية، استخدام AI، أفضل قناة/منتج/خدمة/وكيل، النشاط الأخير (محسوب لكن غير معروض أبدًا — كود ميت فعليًا).

---

# 71. لوحة تحكم الوكيل (Agent Dashboard)

## المطلوب حسب المرجع:
- محادثات مُسنَدة/غير مقروءة، مواعيد اليوم، مهام مُسنَدة، متابعات معلّقة، طابور العملاء، اقتراحات AI، أداء اليوم، زمن الاستجابة، محادثات مكتملة.

## الموجود في السورس:
- `dashboard.service.ts` (`getMyWorkBand`): محادثات مُسنَدة، مهام مُسنَدة، مواعيد اليوم.

## النتيجة:
❌ غير مكتملة.

النواقص:
- محادثات غير مقروءة، متابعات معلّقة، طابور العملاء، اقتراحات AI، أداء اليوم، زمن الاستجابة، محادثات مكتملة — لا شيء من هذا معروض.

---

# 72. الأدوات الحية (Real-Time Widgets)

## المطلوب حسب المرجع:
- تحديث تلقائي بلا تحديث يدوي.

## الموجود في السورس:
- لوحة التحكم والتحليلات مُصيَّرة بالكامل على الخادم (Server Components) بلا أي polling أو WebSocket.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- لا تحديث تلقائي من أي نوع — البيانات تُحمَّل مرة واحدة لكل طلب صفحة.

---

# 73. رؤى الذكاء الاصطناعي (AI Insights)

## المطلوب حسب المرجع:
- شرح ما حدث، الأسباب المحتملة، الإجراءات الموصى بها بلغة بسيطة.

## الموجود في السورس:
- `src/features/ai/services/insights.service.ts`, `insights-panel.tsx`: يولّد رؤى نصية + إجراءات موصى بها من إحصائيات اللوحة، مع تخزين مؤقت 24 ساعة وتحديث يدوي.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع.

---

# 74. مؤشر صحة العمل (Business Health Score)

## المطلوب حسب المرجع:
- 0-100 مبني على: زمن الاستجابة، تحويل leads، نجاح الطلبات، رضا العميل، أداء AI، المحادثات الفائتة، المواعيد، اتجاه الإيراد، نجاح الأتمتة. عرض: ممتاز/جيد/يحتاج انتباه/حرج.

## الموجود في السورس:
- `analytics.service.ts` (`computeHealthScore`): 0-100 مع 4 مستويات نصية مطابقة تمامًا.

## النتيجة:
❌ غير مكتملة.

النواقص:
- مبني على 4 عوامل فقط (تحويل leads، إكمال الطلبات، إكمال المواعيد، نجاح AI) — ينقصه: زمن الاستجابة، المحادثات الفائتة، اتجاه الإيراد، نجاح الأتمتة كمدخلات مباشرة في المعادلة.

---

# 75. تحليلات العملاء المحتملين (Lead Analytics)

## المطلوب حسب المرجع:
- رسوم: leads جديدة، مصادر leads، قمع التحويل، توزيع الـ pipeline، جودة الـ lead، توزيع النقاط، Hot/Lost Leads، معدل الفوز.

## الموجود في السورس:
- `analytics.service.ts`: رسم "leads يوميًا" فقط + توزيع الـ pipeline معروض في لوحة التحكم الرئيسية (وليس صفحة التحليلات).

## النتيجة:
❌ غير مكتملة.

النواقص:
- مصادر leads، قمع التحويل، جودة/توزيع النقاط، Hot/Lost كرسوم بيانية، معدل الفوز كإحصائية ظاهرة — لا شيء منها موجود كرسم تحليلي.

---

# 76. تحليلات المحادثات (Conversation Analytics)

## المطلوب حسب المرجع:
- رسائل/يوم، زمن استجابة متوسط/AI/بشري، مدة المحادثة، متوسط زمن الحل، أكثر الساعات/الأيام نشاطًا، توزيع القناة، توزيع اللغة.

## الموجود في السورس:
- `analytics.service.ts`: توزيع القناة فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- كل بقية المقاييس السبعة المذكورة — لا شيء منها موجود.

---

# 77. تحليلات المبيعات (Sales Analytics)

## المطلوب حسب المرجع:
- الإيراد، متوسط قيمة الطلب، أفضل منتجات/خدمات، الطلبات حسب الحالة، معدل الاسترجاع، العملاء المتكررون، نمو المبيعات.

## الموجود في السورس:
- `analytics.service.ts`: الإيراد، أفضل المنتجات، الطلبات حسب الحالة.

## النتيجة:
❌ غير مكتملة.

النواقص:
- متوسط قيمة الطلب، أفضل الخدمات، معدل الاسترجاع، العملاء المتكررون، نمو المبيعات — غير موجودة.

---

# 78. تحليلات المواعيد (Appointment Analytics)

## المطلوب حسب المرجع:
- مواعيد/يوم، مكتملة/ملغاة/لم يحضر، متوسط وقت الحجز، أكثر الخدمات طلبًا.

## الموجود في السورس:
- `analytics.service.ts`: توزيع حسب الحالة فقط (يشتق منه مكتمل/ملغى/لم يحضر).

## النتيجة:
❌ غير مكتملة.

النواقص:
- مواعيد/يوم كاتجاه زمني، متوسط وقت الحجز، أكثر الخدمات طلبًا — غير موجودة.

---

# 79. تحليلات الذكاء الاصطناعي (AI Analytics)

## المطلوب حسب المرجع:
- الطلبات، الردود، استخدام المزوّد، متوسط الكمون/التكلفة، معدل fallback، معدل التحويل البشري، استخدام المعرفة، معدل النجاح، تقارير الاختلاق، رضا العميل.

## الموجود في السورس:
- `analytics.service.ts`: عدد الطلبات، معدل النجاح فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- استخدام المزوّد (مخفي عمدًا حسب قاعدة "No AI Terminology")، متوسط الكمون/التكلفة، معدل fallback، معدل التحويل البشري (محسوب داخليًا كـ`needsHumanCount` لكن غير معروض)، استخدام المعرفة، تقارير الاختلاق، رضا العميل — غير موجودة.

---

# 80. تحليلات القنوات (Channel Analytics)

## المطلوب حسب المرجع:
- WhatsApp/Instagram: الرسائل، leads، الطلبات، الإيراد، زمن الاستجابة، معدل التحويل.

## الموجود في السورس:
- `analytics.service.ts`: عدد الرسائل/المحادثات حسب القناة فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- leads، الطلبات، الإيراد، زمن الاستجابة، معدل التحويل — لا شيء منها مُقسَّم حسب القناة.

---

# 81. تحليلات العملاء (Customer Analytics)

## المطلوب حسب المرجع:
- عملاء جدد، عائدون، VIP، القيمة الدائمة، معدل الاحتفاظ، معدل فقدان العملاء، متوسط تكرار الشراء، متوسط عدد المحادثات.

## الموجود في السورس:
- لا يوجد — لا تطابق لأي من هذه المقاييس في `src/features/analytics`. (قائمة "مخاطر فقدان العملاء" موجودة لكن في ميزة Campaigns منفصلة تمامًا، وليست جزءًا من التحليلات).

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل المقاييس الثمانية المذكورة — لا شيء منها موجود في وحدة التحليلات.

---

# 82. التقارير (Reports)

## المطلوب حسب المرجع:
- تقارير لـ: المبيعات، الطلبات، المواعيد، المحادثات، العملاء المحتملين، العملاء، AI، القنوات، أداء الفريق، الإيراد، الأتمتة.

## الموجود في السورس:
- `src/app/api/reports/{ai-usage,appointments,automations,contacts,conversations,leads,orders,revenue,team-performance}/route.ts`: 8 من 11 نوعًا موجودة فعليًا.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تقرير "مبيعات" موحّد (فقط Orders/Revenue منفصلين)، لا تقرير "عملاء" تحليلي (فقط تصدير Contacts خام)، لا تقرير "قنوات" مخصص.

---

# 83. التصدير (Exports)

## المطلوب حسب المرجع:
- PDF, Excel, CSV, تصدير API مستقبلي.

## الموجود في السورس:
- `src/lib/{csv,excel,pdf}.ts`, `report-response.ts`: الصيغ الثلاث مدعومة عبر آلية موحّدة.

## النتيجة:
❌ غير مكتملة.

النواقص:
- تقرير "Conversations" يدعم CSV فقط دون Excel/PDF (خلافًا لبقية التقارير)، لا تصدير API (JSON) منفصل.

---

# 84. فلاتر التقارير (Report Filters)

## المطلوب حسب المرجع:
- اليوم، أمس، آخر 7/30 يومًا، هذا/الشهر الماضي، مدى مخصص، القناة، الوكيل، اللغة، وحدة العمل، الوسوم، المنتجات، الخدمات.

## الموجود في السورس:
- `date-range-select.tsx`: 7 أيام/30 يومًا/90 يومًا/الشهر الحالي فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا "اليوم"/"أمس" كخيارات دقيقة، لا مدى مخصص، ولا أي فلتر حسب القناة/الوكيل/اللغة/الوسوم/المنتج/الخدمة على أي تقرير أو صفحة تحليلات.

---

# 85. توصيات الذكاء الاصطناعي — لوحة التحكم (AI Recommendations)

## المطلوب حسب المرجع:
- أمثلة: "تواصل مع هؤلاء 5 عملاء ساخنين اليوم"، "أعد تخزين المنتج X"، "زد عدد الموظفين بعد 5 مساءً".

## الموجود في السورس:
- `dashboard.service.ts` (`hotLeadItems`): توصية "تواصل مع العملاء الساخنين" موجودة فعليًا (مبنية على نقاط الـ lead الحتمية، وليست توليدًا حرًا من AI).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا توصية "أعد تخزين المنتج X" (لا مخزون أصلًا في النظام)، لا توصية "زد عدد الموظفين" (لا تحليل توقيت/تحميل موظفين).

---

# 86. إشعارات لوحة التحكم (Dashboard Notifications)

## المطلوب حسب المرجع:
- معالم الإيراد، فشل AI، مشاكل الاتصال، مشاكل الدفع، أعضاء فريق غير نشطين، طلبات كبيرة، عملاء VIP.

## الموجود في السورس:
- `notifications.ts`: الأنواع الموجودة فقط `subscription_expiring/suspended/automation/crm_followup/support_ticket_reply`.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل الأنواع السبعة المذكورة — لا شيء منها موجود كنوع إشعار.

---

# 87. خطط الاشتراك (Subscription Plans)

## المطلوب حسب المرجع:
- خطط افتراضية: Free Trial, Starter, Professional, Business, Enterprise. كل خطة: سعر شهري/سنوي، حد أقصى مستخدمين/وكلاء AI/قنوات/محادثات/تخزين/ملفات معرفة/سير عمل أتمتة/استدعاءات API/تكاملات، أعلام ميزات.

## الموجود في السورس:
- `src/db/schema/plans.ts`: اسم، دورة فوترة، سعر، عملة، قائمة ميزات مفعّلة (`enabledFeatures[]`).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا خطط افتراضية مزروعة (seeded) بالأسماء المذكورة، **لا أي من حدود الاستخدام الرقمية العشرة المذكورة** (مستخدمين، وكلاء AI، قنوات، محادثات، تخزين، إلخ) — فقط أعلام تفعيل/تعطيل ميزات كاملة، لا حدود كمّية.

---

# 88. الفترة التجريبية المجانية (Free Trial)

## المطلوب حسب المرجع:
- 14 يومًا، بلا بطاقة ائتمان، انتهاء تلقائي، تذكيرات ترقية، تتبّع التحويل.

## الموجود في السورس:
- `workspaces.subscriptionStatus` يبدأ "trial"، `subscription-check.service.ts`: تذكيرات 3/2/1 أيام، تعليق تلقائي عند الانتهاء.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا مدة "14 يومًا" مفروضة في الكود (المدة يحددها المشرف يدويًا لكل حالة)، لا تتبّع معدل تحويل تجريبي->مدفوع.

---

# 89. دورة حياة الاشتراك (Subscription Lifecycle)

## المطلوب حسب المرجع:
- Trial -> Active -> Past Due -> Grace Period -> Suspended -> Cancelled -> Expired (7 حالات). ترقية/تخفيض بلا فقدان بيانات.

## الموجود في السورس:
- `subscriptionStatusEnum = ["trial","active","suspended"]` — 3 حالات فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- Past Due، Grace Period، Cancelled، Expired — 4 من 7 حالات غير موجودة إطلاقًا. الترقية/التخفيض يدوي بالكامل من طرف المشرف، لا مسار ذاتي للعميل.

---

# 90. أعلام الميزات (Feature Flags)

## المطلوب حسب المرجع:
- كل ميزة مدفوعة محكومة بعلم: قناة Instagram، وكلاء AI متعددون، أتمتة، تقارير، تحليلات متقدمة، وصول API، سوق تطبيقات، Voice AI، White Label، ميزات مؤسسية.

## الموجود في السورس:
- `feature-flags.ts` (علم عام واحد فعلي: مولد سير العمل بالـ AI) + `plans.enabledFeatures` (12 مفتاح وحدة: inbox/contacts/leads/orders/appointments/automations/knowledge_base/analytics/website/integrations/campaigns/ads).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا أعلام مسمّاة بالضبط كما في المرجع (Instagram Channel, Multiple AI Agents, Voice AI, White Label, Marketplace, Enterprise Features) — الموجود أعلام على مستوى الوحدة الكاملة (module) وليس ميزات فرعية دقيقة.

---

# 91. تتبع الاستخدام (Usage Tracking)

## المطلوب حسب المرجع:
- طلبات AI، Tokens، الرسائل، المحادثات، الطلبات، المواعيد، التخزين، طلبات API، تنفيذات الأتمتة، مستندات المعرفة، القنوات المتصلة.

## الموجود في السورس:
- `ai-usage.ts` يتتبّع طلبات/Tokens AI فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا عدّاد لأي من: الرسائل، المحادثات، الطلبات، المواعيد، التخزين، طلبات API، تنفيذات الأتمتة، مستندات المعرفة، القنوات المتصلة.

---

# 92. المدفوعات (Payments)

## المطلوب حسب المرجع:
- تجريد لمزوّد الدفع يدعم عدة مزوّدين (AsiaHawala, ZainCash, Stripe).

## الموجود في السورس:
- لا يوجد — التفعيل يدوي بالكامل من طرف مشرف بعد تواصل واتساب مع العميل (موثَّق في تعليق الكود وفي `DEFERRED_TASKS.md`، بانتظار تأسيس شركة رسمية).

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل شيء: تجريد المزوّد، أي بوابة دفع فعلية.

---

# 93. تدفق الدفع (Payment Flow)

## المطلوب حسب المرجع:
- اختيار خطة -> مراجعة -> اختيار طريقة دفع -> إتمام -> تحقق -> تفعيل -> فاتورة -> إشعار.

## الموجود في السورس:
- `src/app/dashboard/billing/page.tsx`: رابط واتساب فقط يطلب من العميل التواصل يدويًا.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل خطوات التدفق الثماني — لا شيء منها داخل التطبيق (التفعيل إجراء يدوي من المشرف).

---

# 94. الفواتير (Invoices)

## المطلوب حسب المرجع:
- توليد تلقائي: رقم الفاتورة، Workspace، الخطة، المبلغ، العملة، الضرائب، حالة الدفع، تاريخ الدفع/الاستحقاق، تنزيل PDF.

## الموجود في السورس:
- لا يوجد جدول `invoices` في قاعدة البيانات إطلاقًا.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 95. سجل الفوترة (Billing History)

## المطلوب حسب المرجع:
- فواتير سابقة، الاشتراك الحالي، سجل الدفع، مدفوعات فاشلة، تجديدات، تغييرات الخطة، سجل الاستخدام.

## الموجود في السورس:
- `billing/page.tsx`: الخطة الحالية، الحالة، تاريخ الانتهاء فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- فواتير سابقة، سجل الدفع، مدفوعات فاشلة، سجل التجديدات، سجل تغييرات الخطة، سجل الاستخدام — لا شيء منها موجود.

---

# 96. إدارة الخطة (Plan Management)

## المطلوب حسب المرجع:
- العميل يستطيع: ترقية، تخفيض، إلغاء، استئناف، تجديد، تبديل شهري/سنوي، مقارنة الخطط.

## الموجود في السورس:
- لا شيء — كل تغيير خطة إجراء إداري (Super Admin) فقط.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة من جهة العميل).

النواقص:
- كل الإجراءات الستة المذكورة غير متاحة للعميل نفسه؛ إدارية بالكامل.

---

# 97. تحذيرات الاستخدام (Usage Warnings)

## المطلوب حسب المرجع:
- 80% محادثات، 90% تخزين، 95% استخدام AI، انتهاء تجربة خلال 3 أيام، انتهاء اشتراك غدًا.

## الموجود في السورس:
- `subscription-check.service.ts`: تذكيرات انتهاء الاشتراك/التجربة عند 3/2/1 أيام فعليًا.

## النتيجة:
❌ غير مكتملة.

النواقص:
- تحذيرات 80%/90%/95% للاستخدام غير موجودة إطلاقًا (لا حدود استخدام مُعرَّفة أصلًا لمقارنتها بها).

---

# 98. لوحة إيرادات المالك (Owner Revenue Dashboard)

## المطلوب حسب المرجع:
- MRR, ARR, اشتراكات جديدة، تجديدات، إلغاءات، معدل فقدان العملاء، LTV, ARPU, معدل تحويل تجريبي، أفضل الخطط، فشل الدفع.

## الموجود في السورس:
- `src/app/admin/revenue/page.tsx`, `revenue.ts`: MRR, ARR، عدد الاشتراكات النشطة/المُسعَّرة، توزيع حسب الخطة.

## النتيجة:
❌ غير مكتملة.

النواقص:
- اشتراكات جديدة، تجديدات، إلغاءات، معدل فقدان العملاء، LTV، ARPU، معدل التحويل التجريبي، فشل الدفع — لا شيء منها محسوب.

---

# 99. الخصومات (Discounts)

## المطلوب حسب المرجع:
- أكواد كوبون، خصم نسبي، خصم ثابت، تمديد تجربة، مكافآت إحالة (مستقبلي).

## الموجود في السورس:
- لا يوجد أي كود متعلق بالكوبونات/الخصومات في كامل المشروع.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 100. المبالغ المستردة (Refunds)

## المطلوب حسب المرجع:
- تتبّع: طُلب، اعتُمد، رُفض، اكتمل، السبب.

## الموجود في السورس:
- لا يوجد (لا نظام دفع أصلًا لاسترجاع مبالغ منه).

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 101. بنية الضرائب (Tax Architecture)

## المطلوب حسب المرجع:
- محرك ضرائب معياري قابل للتوسع حسب الدولة مستقبلًا.

## الموجود في السورس:
- لا حقول ضريبة ولا محرك من أي نوع في `plans` أو `orders`.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 102. تعدد العملات (Multi-Currency)

## المطلوب حسب المرجع:
- تجهيز لـ IQD, USD, SAR, AED, KWD مع تجريد لتحويل العملة.

## الموجود في السورس:
- `plans.currency`: عمود نصي حر افتراضيه "IQD"، بلا قائمة عملات مُقفَلة أو منطق تحويل.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا قائمة عملات مدعومة رسميًا، لا منطق تحويل/تنسيق عملة، لا اختيار عملة لكل Workspace.

---

# 103. إشعارات الفوترة (Billing Notifications)

## المطلوب حسب المرجع:
- دفع ناجح/فاشل، فاتورة صدرت، تجربة تنتهي، اشتراك جُدِّد/رُقِّي/خُفِّض، حد استخدام وصل.

## الموجود في السورس:
- `subscription_expiring`, `subscription_suspended` فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- بقية الأنواع الستة — غير موجودة (لا دفع حقيقي أصلًا لإصدار إشعارات دفع عنه).

---

# 104. تقارير الفوترة (Billing Reporting)

## المطلوب حسب المرجع:
- تقارير: الإيراد، MRR، ARR، الاشتراكات، المدفوعات، الاسترجاعات، أداء المزوّد، تكلفة AI، الربحية.

## الموجود في السورس:
- MRR/ARR/توزيع حسب الخطة فقط (`/admin/revenue`).

## النتيجة:
❌ غير مكتملة.

النواقص:
- تقارير الاشتراكات، المدفوعات، الاسترجاعات، أداء المزوّد، تكلفة AI، الربحية — لا شيء منها موجود.

---

# 105. مصادقة المشرف الأعلى (Super Admin Authentication)

## المطلوب حسب المرجع:
- أدوار: Owner, Administrator, Support Agent, Finance, Developer, Read Only. جاهزية MFA، إدارة جلسات، تتبّع IP، أجهزة موثوقة.

## الموجود في السورس:
- `requirePlatformAdmin`/`requirePrimaryPlatformAdmin`: طبقتان فقط — قائمة بريد بيئية (bootstrap) أو صف في جدول `platform_admins` (تمييز ثنائي فقط).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا أدوار متعددة (Administrator/Support Agent/Finance/Developer/Read Only) — فقط "مشرف" أو "لا"، لا MFA، لا تتبّع IP، لا أجهزة موثوقة.

---

# 106. لوحة تحكم المشرف الأعلى (Super Admin Dashboard)

## المطلوب حسب المرجع:
- إجمالي Workspaces، عملاء نشطون/تجريبيون، MRR، ARR، نمو الإيراد، تسجيلات يومية، Workspaces نشطة يوميًا، طلبات/تكاليف AI، متوسط زمن الاستجابة، تذاكر دعم، حالة البنية التحتية، القنوات المتصلة، صحة الـ webhooks، حالة الطوابير، استخدام التخزين، استخدام API، الحوادث الأخيرة.

## الموجود في السورس:
- `src/app/admin/page.tsx`: إجمالي/نشط/تجريبي/معلَّق Workspaces، MRR/ARR، تسجيلات حديثة (قائمة)، عدد طلبات AI ومعدل النجاح.

## النتيجة:
❌ غير مكتملة.

النواقص:
- نمو الإيراد (اتجاه)، تسجيلات يومية (كرقم/رسم)، Workspaces نشطة يوميًا، تكاليف AI، متوسط زمن استجابة الدعم، تذاكر الدعم كعدّاد، حالة البنية التحتية، القنوات المتصلة، صحة الـ webhooks، حالة الطوابير، استخدام التخزين/API، الحوادث الأخيرة — لا شيء منها معروض.

---

# 107. إدارة مساحات العمل (Workspace Management)

## المطلوب حسب المرجع:
- بحث، فلاتر، تعليق، استعادة، حذف، انتحال هوية آمن، إعادة تعيين تجربة/حدود، ترقية/تخفيض خطة، نقل الملكية، عرض السجلات/الاستخدام/تكاليف AI/النشاط.

## الموجود في السورس:
- `src/app/admin/workspaces/**`: بحث نصي، تعليق/استئناف/تفعيل خطة، عرض للقراءة فقط (Impersonate آمن حقًا — للقراءة فقط، مُسجَّل بالتدقيق).

## النتيجة:
❌ غير مكتملة.

النواقص:
- فلاتر (حالة/خطة)، حذف Workspace، إعادة تعيين تجربة/حدود، نقل الملكية، عرض تكاليف AI الفعلية (بالدولار) — لا شيء منها موجود.

---

# 108. دعم العملاء — أدوات الإدارة (Customer Support Tools)

## المطلوب حسب المرجع:
- بحث عميل، عرض المحادثات/سجلات AI/الاشتراكات/الفواتير/تذاكر الدعم/الأخطاء/التكاملات/القنوات المتصلة، بلا كشف أسرار.

## الموجود في السورس:
- عرض قراءة فقط لبيانات CRM لكل Workspace (جهات اتصال/leads/طلبات/مواعيد/محادثات).

## النتيجة:
❌ غير مكتملة.

النواقص:
- بحث عميل عام، سجلات AI التفصيلية، الفواتير (غير موجودة أصلًا)، عارض أخطاء، عرض التكاملات/القنوات المتصلة لكل Workspace — لا شيء منها موجود في أدوات الدعم.

---

# 109. تذاكر الدعم (Support Tickets)

## المطلوب حسب المرجع:
- الأولوية، الفئة، الحالة، الموظف المُسنَد، زمن الاستجابة/الحل، السجل، المرفقات، ملاحظات داخلية.

## الموجود في السورس:
- `src/db/schema/support-tickets.ts`: الحالة موجودة، رسائل بين المستأجر والمشرف.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا فئة (Category)، لا موظف مُسنَد، لا قياس زمن استجابة/حل، لا سجل تاريخ الحالة، لا مرفقات، لا نوع "ملاحظة داخلية" منفصل عن الرد العادي.

---

# 110. إدارة الفوترة — لوحة المشرف (Billing Admin)

## المطلوب حسب المرجع:
- إدارة: الخطط، الكوبونات، الفواتير، المدفوعات، الاسترجاعات، التجارب، الأرصدة، التعديلات اليدوية، تقارير الإيراد.

## الموجود في السورس:
- `src/app/admin/plans/page.tsx`: إدارة الخطط فقط (إنشاء/حذف/تسعير).

## النتيجة:
❌ غير مكتملة.

النواقص:
- الكوبونات، الفواتير، المدفوعات، الاسترجاعات، إدارة تجارب منفصلة، الأرصدة، التعديلات اليدوية (أبعد من تفعيل خطة/أيام) — لا شيء منها موجود.

---

# 111. عمليات الذكاء الاصطناعي (AI Operations)

## المطلوب حسب المرجع:
- مراقبة: المزوّدين، استخدام النموذج، الطلبات اليومية، الكمون، الفشل، معدل fallback، التكلفة المقدَّرة، استخدام كل Workspace، حدود المعدل.

## الموجود في السورس:
- `src/app/admin/ai-operations/page.tsx`, `ai-usage/page.tsx`: عدد الطلبات/معدل النجاح/الكمون لكل نموذج/Workspace.

## النتيجة:
❌ غير مكتملة.

النواقص:
- معدل fallback (لا يوجد fallback أصلًا)، التكلفة المقدَّرة، عرض حدود المعدل (Rate Limits) داخل واجهة العمليات — غير موجودة.

---

# 112. التحكم بمزود الذكاء الاصطناعي (AI Provider Control)

## المطلوب حسب المرجع:
- تفعيل/تعطيل كل مزوّد (Claude, OpenAI, Gemini, OpenRouter, Ollama) بشكل مستقل بلا إعادة نشر.

## الموجود في السورس:
- `platform-settings.ts` (`aiEnabled`): مفتاح إيقاف/تشغيل **عام واحد فقط** لكل الذكاء الاصطناعي.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تحكم منفصل لكل مزوّد (لأن مزوّدًا واحدًا فقط — Claude — موجود أصلًا؛ انظر #34).

---

# 113. أعلام الميزات — لوحة المشرف (Feature Flags Admin)

## المطلوب حسب المرجع:
- تفعيل لكامل المنصة، أو Workspaces محددة، أو مستخدمي Beta، أو عملاء مؤسسات، أو اختبار داخلي.

## الموجود في السورس:
- `feature-flags.ts`: علم عام واحد بمستوى المنصة بالكامل فقط (بلا نطاق).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا استهداف لـ Workspaces محددة، Beta، عملاء مؤسسات، أو اختبار داخلي — فقط تشغيل/إيقاف عام.

---

# 114. صحة النظام (System Health)

## المطلوب حسب المرجع:
- مراقبة: حالة API، الطوابير، العمّال (Workers)، قاعدة البيانات، Redis، التخزين، البريد، الـ Webhooks، اتصالات القنوات، المهام الخلفية.

## الموجود في السورس:
- لا صفحة/خدمة مراقبة صحة نظام من أي نوع.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل العناصر العشرة المذكورة — غير موجودة.

---

# 115. مراقبة الأخطاء (Error Monitoring)

## المطلوب حسب المرجع:
- تتبّع: الاستثناءات، أخطاء API، فشل webhook، فشل الطابور، أخطاء AI، أخطاء الدفع، فشل مصادقة، أخطاء قاعدة البيانات.

## الموجود في السورس:
- لا شيء — لا Sentry ولا أي نظام مراقبة أخطاء (موثَّق رسميًا كعنصر مؤجَّل بانتظار حساب Sentry/PostHog في `DEFERRED_TASKS.md`).

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة (بديل جزئي: `console.error` عند نقاط الفشل، تظهر فقط في سجلات Vercel اليدوية).

---

# 116. سجلات التدقيق — لوحة المشرف (Audit Logs Admin)

## المطلوب حسب المرجع:
- تسجيل: دخول المشرف، تعليق Workspace، تغيير الاشتراك، إصدار استرجاع، تفعيل ميزة، تحديث دور، تغيير مزوّد AI، وصول الدعم.

## الموجود في السورس:
- `audit-logs.ts`: يسجّل فعليًا تفعيل/تغيير الاشتراك، إضافة/إزالة مشرف منصة، حفظ/حذف خطة، تغيير إعدادات المنصة، بدء انتحال الهوية، رد/تغيير حالة تذكرة دعم، تغيير تفعيل AI، إنشاء/تبديل علم ميزة.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تسجيل لدخول المشرف نفسه، لا "تحديث دور" (لا أدوار متعددة أصلًا للمشرفين)، لا "تغيير مزوّد AI" (مزوّد واحد فقط)، لا حدث "وصول دعم" منفصل عن الانتحال.

---

# 117. إشعارات المشرف (Admin Notifications)

## المطلوب حسب المرجع:
- تكاليف AI مرتفعة، فشل مزوّد، فشل دفع، حدود تخزين، تنبيهات أمنية، مشاكل بنية تحتية، تسجيلات عملاء كبار، طلبات مؤسسية.

## الموجود في السورس:
- لا يوجد نظام تنبيه للمشرفين من أي نوع.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل الأنواع الثمانية المذكورة — غير موجودة.

---

# 118. تحليلات المنصة (Platform Analytics)

## المطلوب حسب المرجع:
- نمو العملاء/الإيراد، استخدام/تكلفة AI حسب المزوّد، توزيع الخطط، فقدان العملاء، الاحتفاظ، تحويل التجربة، أفضل الصناعات، استخدام القنوات، التوزيع الجغرافي.

## الموجود في السورس:
- MRR/ARR الحالي + توزيع الخطط فقط (`/admin/revenue`)، استخدام AI حسب النموذج/Workspace (بلا تكلفة).

## النتيجة:
❌ غير مكتملة.

النواقص:
- نمو (اتجاه) العملاء/الإيراد، فقدان العملاء، الاحتفاظ، تحويل التجربة، أفضل الصناعات، استخدام القنوات، التوزيع الجغرافي — لا شيء منها موجود.

---
## GROUP 7: إعادة هيكلة التنقل النهائية (Part 13B)
---

# 119. خريطة التنقل بـ 5 أقسام (Product Navigation Map)

## المطلوب حسب المرجع:
- HOME, INBOX, CUSTOMERS, AI EMPLOYEE, GROWTH كأقسام رئيسية بالشريط الجانبي؛ WORKSPACE SETTINGS في قائمة الحساب وليس الشريط الرئيسي؛ SUPER ADMIN تطبيق منفصل تمامًا.

## الموجود في السورس:
- تم التحقق مباشرة من `src/app/dashboard/layout.tsx`: الشريط الجانبي فعليًا مقسَّم لـ HOME/INBOX/CUSTOMERS (Contacts/Leads/Orders/Appointments)/AI EMPLOYEE (Settings/Knowledge Base/Test AI)/GROWTH (Automations/Analytics/Website/Integrations/Campaigns/Ads). Team/Billing/Audit Log/Workspace Profile/Support انتقلت فعليًا لقائمة الحساب المنسدلة.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع.

---

# 120. تخطيط لوحة HOME (3 نطاقات)

## المطلوب حسب المرجع:
- Band 1: ماذا حدث اليوم. Band 2: ما يحتاج انتباهًا (متأخرين، تحويل بشري، مشاكل اتصال بلغة بسيطة، عملاء VIP بمخاطر فقدان). Band 3: كيف ينمو العمل (اتجاه، تحويل، مصادر، أفضل منتجات، أداء حملات، مؤشر الصحة).

## الموجود في السورس:
- `dashboard.service.ts`: نطاق "اليوم" ونطاق "يحتاج انتباه" (متأخرين + تحويل بشري + عملاء ساخنون) موجودان فعليًا. نطاق "النمو" موجود لكن جزئي (مؤشر الصحة فقط، بلا أداء حملات ROAS مدمج).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا "مشاكل اتصال بلغة بسيطة" (لا قنوات متصلة أصلًا لعرض مشاكلها)، لا "عملاء VIP بمخاطر فقدان" مدمجة في لوحة Home (موجودة في صفحة Campaigns منفصلة)، Band 3 ينقصه أداء الحملات/ROAS كما يصف 13B تحديدًا.

---

# 121. تسلسل الإعداد الأولي الجديد (Onboarding Sequence — Part 13B)

## المطلوب حسب المرجع:
- 7 خطوات: تسجيل (Google أساسي) -> بيانات العمل -> تعريف الوكيل (اسم/نبرة/إبداع بلغة بسيطة) -> تعليمه العمل -> ربط القنوات -> تفعيل فوري مع الاعتراف بالنقص -> احتفال بأول رسالة حقيقية.

## الموجود في السورس:
- خطوات 1-4 موجودة وتطابق الترتيب. الوكيل يبدأ العمل فورًا بأي معرفة متاحة، ويعترف بعدم المعرفة عند النقص (يطابق الخطوة 6).

## النتيجة:
❌ غير مكتملة.

النواقص:
- خطوة "ربط القنوات" غير وظيفية (كما في #9)، لا احتفال خاص مُصمَّم لأول رسالة عميل حقيقية (Success Feedback العام موجود لمعالم أخرى، لكن ليس هذا الحدث تحديدًا).

---

# 122. إعدادات مساحة العمل (Workspace Settings)

## المطلوب حسب المرجع:
- Team & Roles، Channels، Billing، Integrations، Workspace Profile، Audit Log، API Keys/Webhooks (متقدّم/افتراضيًا مخفي).

## الموجود في السورس:
- قائمة الحساب تحتوي فعليًا: Workspace Profile، Team، Billing، Audit Log، Support. صفحة Integrations منفصلة تحت GROWTH وليس هنا (انحراف تصنيفي بسيط).

## النتيجة:
❌ غير مكتملة.

النواقص:
- "Channels" كصفحة إدارة منفصلة داخل Workspace Settings غير موجودة (لا شيء لإدارته أصلًا). Integrations مصنّفة تحت GROWTH بدل WORKSPACE SETTINGS كما يحدد 13B تحديدًا.

---

# 123. الإخفاء عن المستأجرين (Hidden / Never-Tenant-Facing)

## المطلوب حسب المرجع:
- Router/Provider AI، أسماء النماذج، تفاصيل التكلفة/tokens، صحة النظام، مراقبة الأخطاء، أعلام الميزات — يجب أن تبقى حصرًا داخل تطبيق Super Admin منفصل تمامًا، أبدًا داخل تطبيق المستأجر.

## الموجود في السورس:
- تم التحقق: لا تسرّب لأي مصطلح تقني (Model/Provider/Token/Temperature) في أي واجهة أو ملف ترجمة تابع للمستأجر. `/admin/**` محمي بـ `requirePlatformAdmin()` مستقل عن جلسة تطبيق المستأجر.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع.

---
## GROUP 8: الوحدات النمائية (Parts 14-17)
---

# 124. الإنشاء التلقائي للموقع (Automatic Website Creation)

## المطلوب حسب المرجع:
- توليد subdomain تلقائي من اسم العمل عند إنشاء Workspace، قابل للتعديل من المستخدم، بلا نطاقات مخصصة (بالتصميم).

## الموجود في السورس:
- `workspace.service.ts`: توليد slug فريد تلقائيًا، صف متجر واحد يُنشأ تلقائيًا لكل Workspace.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا إمكانية للمستخدم لتعديل الـ slug بعد إنشائه — لا إجراء/واجهة لذلك في أي مكان.

---

# 125. معالج إعداد الموقع (Website Setup Wizard)

## المطلوب حسب المرجع:
- جمع فوري بعد التسجيل: الشعار، الغلاف، الألوان الأساسية/الثانوية، الوصف، الهاتف، العنوان، روابط التواصل.

## الموجود في السورس:
- لا معالج (wizard) مخصص من أي نوع — الحقول (إن وُجدت) تُعدَّل لاحقًا يدويًا من محرر عادي بصفحة `/dashboard/website`.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- المعالج نفسه غير موجود؛ الشعار، الغلاف، اللون الثانوي، روابط التواصل غير مجمَّعة في أي مكان (لا كمعالج ولا حتى كحقول لاحقة).

---

# 126. تخصيص الموقع (Website Customization)

## المطلوب حسب المرجع:
- الاسم، الشعار، Favicon، لونان أساسي/ثانوي، الخطوط، الأزرار، الزوايا المدوّرة، نمط الترويسة/التذييل، بانر الصفحة الرئيسية، القالب، الوضع الليلي.

## الموجود في السورس:
- `storefront-editor.tsx`, `storefronts.ts`: لون أساسي واحد فقط، نص hero، نص "عن العمل"، هاتف تواصل.

## النتيجة:
❌ غير مكتملة.

النواقص:
- اسم العمل (غير قابل للتعديل هنا)، الشعار، Favicon، اللون الثانوي، الخطوط، الأزرار، الزوايا المدوّرة، نمط الترويسة/التذييل، بانر صورة حقيقي، القالب، الوضع الليلي — لا شيء من هذا موجود.

---

# 127. القوالب (Themes)

## المطلوب حسب المرجع:
- قوالب مصمَّمة احترافيًا متعددة (Modern Store, Luxury, Minimal, Restaurant, Medical, Beauty, Real Estate, Education, Electronics, Fashion, Corporate).

## الموجود في السورس:
- لا مفهوم "قالب" إطلاقًا — صفحة واحدة بتخطيط ثابت لكل المتاجر.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل القوالب الأحد عشر — غير موجودة.

---

# 128. الصفحات (Pages)

## المطلوب حسب المرجع:
- توليد تلقائي: Home, Products, About, Contact, FAQ, Privacy Policy, Terms, Thank You, 404. إخفاء/إعادة ترتيب.

## الموجود في السورس:
- `src/app/store/[slug]/page.tsx`: صفحة تمرير واحدة فقط تحتوي كل الأقسام.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة كصفحات منفصلة).

النواقص:
- لا صفحات منفصلة (Products/About/Contact/FAQ/Privacy/Terms/Thank You/404)، لا إخفاء/إعادة ترتيب.

---

# 129. باني الأقسام (Section Builder)

## المطلوب حسب المرجع:
- أقسام قابلة للإضافة/الحذف/إعادة الترتيب: Hero, Featured Products, Categories, Collections, Special Offers, Testimonials, Gallery, Video, Statistics, FAQ, Contact Form, Map, Newsletter, Footer. سحب وإفلات.

## الموجود في السورس:
- أقسام ثابتة مبرمَجة يدويًا (hero, about, products, services, contact) بترتيب ثابت، بلا أي إعداد.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل الأقسام الـ14 المذكورة كوحدات قابلة للتحكم، وميزة السحب والإفلات — غير موجودة.

---

# 130. كتالوج المنتجات — الموقع (Product Catalog on Website)

## المطلوب حسب المرجع:
- المنتجات تُدار من المنصة الرئيسية وتظهر تلقائيًا على الموقع، بلا ازدواجية بيانات.

## الموجود في السورس:
- `src/app/store/[slug]/page.tsx`: يقرأ مباشرة من `productRepository`/`serviceRepository` الحقيقيين، مفلترًا حسب `isActive`.

## النتيجة:
✅ مكتملة 100% كما تم ذكرها في ملف المرجع.

---

# 131. عرض المنتجات (Product Display)

## المطلوب حسب المرجع:
- تخطيطات متعددة: شبكة، قائمة، Carousel، مميّز، الأكثر مبيعًا، الأحدث، المخفَّض — قابلة للتبديل.

## الموجود في السورس:
- تخطيط شبكي ثابت واحد فقط لكل المنتجات النشطة.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا قائمة، Carousel، مميّز، الأكثر مبيعًا، الأحدث، المخفَّض — ولا أي مبدّل تخطيط.

---

# 132. صفحة المنتج (Product Page)

## المطلوب حسب المرجع:
- صفحة مخصصة لكل منتج: صور، معرض، سعر، خصم، وصف، مواصفات، منتجات ذات صلة، أزرار مشاركة/طلب/واتساب/مساعد AI.

## الموجود في السورس:
- لا صفحة منتج فردية إطلاقًا — فقط بطاقة داخل الصفحة الرئيسية.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الصفحة نفسها وكل عناصرها الثمانية — غير موجودة.

---

# 133. البحث والتصفية — الموقع (Search & Filtering)

## المطلوب حسب المرجع:
- بحث، فئات، فلتر سعر، الأحدث، الأكثر مبيعًا، الخصومات، التوفر.

## الموجود في السورس:
- لا شيء — لا صندوق بحث ولا فلاتر في صفحة المتجر العامة.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل السبعة المذكورة — غير موجودة.

---

# 134. نظام الطلبات — الموقع (Order System)

## المطلوب حسب المرجع:
- الزائر يطلب مباشرة: الاسم، الهاتف، العنوان، الملاحظات، المنتجات المختارة، الكمية.

## الموجود في السورس:
- `inquiry-form.tsx`: يجمع فقط الاسم/الهاتف/رسالة نصية حرة — بلا اختيار منتج أو كمية.

## النتيجة:
❌ غير مكتملة (فعليًا نظام "استفسار" وليس نظام طلب).

النواقص:
- اختيار المنتجات، الكمية، العنوان كحقل منفصل — لا شيء من هذا موجود؛ لا مفهوم سلة/دفع.

---

# 135. تدفق الطلب — الموقع (Order Flow)

## المطلوب حسب المرجع:
- الطلب المُرسَل ينشئ تلقائيًا: طلب -> عميل -> سجل CRM -> إشعار -> سجل نشاط -> حدث تحليلات -> محفّز أتمتة.

## الموجود في السورس:
- `storefront.service.ts` (`submitInquiry`): ينشئ جهة اتصال + Lead (وليس Order)، يسجّل نشاطًا، يُطلق أتمتة.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا كيان "طلب" (Order) يُنشأ فعليًا (لأن لا نظام طلبات حقيقي بالموقع — انظر #134)، لا إشعار مباشر، لا حدث تحليلات مخصص.

---

# 136. مساعد الموقع بالذكاء الاصطناعي (AI Website Assistant)

## المطلوب حسب المرجع:
- زوار يسألون عن المنتجات/الأسعار/التوفر/التوصيات/معلومات العمل/السياسات/الأسئلة الشائعة، باستخدام قاعدة معرفة Workspace.

## الموجود في السورس:
- لا يوجد أي مكوّن محادثة/مساعد على صفحة المتجر العامة.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 137. التوصيات الذكية — الموقع (Smart Product Recommendations)

## المطلوب حسب المرجع:
- توصية منتجات بناءً على الأسئلة، الميزانية، الاهتمامات، المحادثة السابقة، الأكثر شراءً.

## الموجود في السورس:
- لا منطق توصية من أي نوع على صفحة المتجر.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 138. العروض الترويجية (Promotions)

## المطلوب حسب المرجع:
- شريط إعلانات، بانر رئيسي، منتجات مميّزة، عروض خاصة، عدّاد تنازلي، عروض محدودة المدة.

## الموجود في السورس:
- لا حقول ترويجية في الـ schema ولا أقسام مقابلة في الصفحة.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل الستة المذكورة — غير موجودة.

---

# 139. باني النوافذ المنبثقة (Popup Builder)

## المطلوب حسب المرجع:
- نوافذ خصم/كوبون/إعلان/نشرة إخبارية/عرض خاص، بمحفّزات: أول زيارة، تأخير زمني، نية الخروج، يدوي.

## الموجود في السورس:
- لا يوجد أي كود متعلق بالنوافذ المنبثقة في كامل المشروع.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 140. النماذج (Forms)

## المطلوب حسب المرجع:
- نموذج تواصل، طلب عرض سعر، طلب موعد، اشتراك نشرة، طلب دعم — كل نوع مستقل، وكل إرسال ينشئ نشاط CRM.

## الموجود في السورس:
- `inquiry-form.tsx`: نموذج استفسار عام واحد فقط، ينشئ فعليًا نشاط CRM.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تمييز بين خمسة أنواع نماذج مستقلة (عرض سعر/موعد/نشرة/دعم) — نموذج واحد عام فقط.

---

# 141. روابط التواصل الاجتماعي (Social Links)

## المطلوب حسب المرجع:
- WhatsApp, Instagram, Facebook, TikTok, YouTube, Snapchat, Telegram, هاتف, بريد.

## الموجود في السورس:
- لا حقول روابط تواصل اجتماعي في `storefronts` schema (فقط حقل هاتف تواصل عادي).

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل الروابط الثمانية المذكورة كأزرار/حقول — غير موجودة.

---

# 142. Meta Pixel والتتبع (Meta Pixel & Tracking)

## المطلوب حسب المرجع:
- Meta Pixel, Google Analytics, GTM, Google Ads, TikTok Pixel, Snap Pixel, LinkedIn Insight, Microsoft Clarity — إعداد بلا كود.

## الموجود في السورس:
- لا حقول تتبّع ولا سكربتات مُدرَجة في صفحة المتجر.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل الثمانية المذكورة — غير موجودة.

---

# 143. تحسين محركات البحث (SEO)

## المطلوب حسب المرجع:
- عنوان SEO، وصف Meta، Open Graph، Twitter Cards، روابط ودّية، Canonical URLs، بيانات منظّمة، Sitemap، Robots.txt.

## الموجود في السورس:
- لا `generateMetadata`/`metadata` في صفحة المتجر، لا sitemap/robots مخصصين لمسارات المتجر.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل التسعة المذكورة — غير موجودة (الـ slug نفسه "ودّي" لكن بلا إدارة SEO حقيقية حوله).

---

# 144. التقييمات والشهادات (Reviews & Testimonials)

## المطلوب حسب المرجع:
- تقييمات يدوية، شهادات CRM، تقييمات مميّزة، تقييم نجوم، صور.

## الموجود في السورس:
- لا حقول أو مكوّنات تقييمات/شهادات في أي مكان بوحدة المتجر.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 145. المدونة (Blog)

## المطلوب حسب المرجع:
- مدونة بسيطة، AI يولّد مقالات/أسئلة شائعة/أوصاف منتجات/فئات/محتوى SEO.

## الموجود في السورس:
- لا يوجد أي كود متعلق بالمدونة في كامل المشروع.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 146. تحليلات الموقع (Website Analytics)

## المطلوب حسب المرجع:
- الزوار، مشاهدات الصفحة/المنتج، الطلبات، معدل التحويل، أفضل المنتجات، مصادر الزيارات، أكثر الصفحات زيارة، كلمات البحث، نقرات الأزرار، إرسالات النماذج.

## الموجود في السورس:
- لا تتبّع/تجميع لأي بيانات زوار في وحدة المتجر؛ وحدة التحليلات العامة بالمنصة لا تربط ببيانات زوار الموقع.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل العشرة المذكورة — غير موجودة.

---

# 147. تجربة الموبايل — الموقع (Mobile Experience)

## المطلوب حسب المرجع:
- Mobile-first، متجاوب، سريع، ملائم للمس، صور مُحسَّنة.

## الموجود في السورس:
- استخدام فعلي لفئات Tailwind المتجاوبة (`sm:`, `lg:`, إلخ) في صفحة المتجر.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تحسين خاص للصور (استخدام `<img>` عادي وليس `next/image`)، لا عمل تحسين موبايل مخصص أبعد من التجاوب الافتراضي.

---

# 148. تعدد اللغات — الموقع (Multi-Language)

## المطلوب حسب المرجع:
- عربي، إنجليزي، كردي، RTL/LTR.

## الموجود في السورس:
- نصوص واجهة المتجر مترجمة عبر `next-intl` (`website.public.*`).

## النتيجة:
❌ غير مكتملة.

النواقص:
- محتوى المتجر نفسه (العنوان، الوصف، أسماء المنتجات) نص واحد بلا نسخ متعددة اللغات — فقط نصوص الواجهة الثابتة مترجمة، وليس محتوى العمل الفعلي.

---

# 149. بنية التكاملات (Integration Architecture)

## المطلوب حسب المرجع:
- طبقة مخصصة: Application -> Integration Manager -> Connector Interface -> Provider Adapter -> External Service. كل تكامل ينفّذ نفس الواجهة.

## الموجود في السورس:
- خدمة مسطّحة واحدة (`integration.service.ts`) تستدعي المستودعات مباشرة — بلا واجهة Connector أو طبقة Adapter.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة كبنية مجرّدة).

النواقص:
- Integration Manager، Connector Interface، Provider Adapter — لا شيء منها موجود كبنية مستقلة.

---

# 150. مدير التكاملات (Integration Manager)

## المطلوب حسب المرجع:
- تفعيل/تعطيل، تخزين آمن لبيانات الاعتماد، مراقبة صحة الاتصال، إعادة محاولة الفشل، إدارة webhooks، سجلات، مراقبة الاستخدام، سجل مزامنة.

## الموجود في السورس:
- CRUD لمفاتيح API واشتراكات webhook فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- مراقبة صحة الاتصال، إعادة محاولة الفشل، سجلات التسليم، مراقبة الاستخدام، سجل مزامنة — لا شيء منها موجود.

---

# 151. تكاملات الشحن (Shipping Integrations)

## المطلوب حسب المرجع:
- أي مزوّد شحن (Aramex, DHL, FedEx, UPS، محلي) — إنشاء/إلغاء/تتبّع شحنة، حالة، ملصقات، تكلفة، تحديثات تلقائية.

## الموجود في السورس:
- لا يوجد أي كود متعلق بالشحن في كامل المشروع.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 152. سير عمل الطلب إلى الشحن (Order to Shipping Workflow)

## المطلوب حسب المرجع:
- طلب -> CRM -> إشعار -> مراجعة المالك -> "إنشاء شحنة" -> إرسال بيانات للمزوّد -> استرجاع رقم تتبّع/تكلفة/تاريخ -> تحديث الطلب.

## الموجود في السورس:
- لا يوجد — لا فعل "إنشاء شحنة" ولا استدعاء لمزوّد شحن في `order.service.ts`.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- التدفق بالكامل غائب.

---

# 153. الإنشاء التلقائي الاختياري للشحنة (Optional Automatic Shipment Creation)

## المطلوب حسب المرجع:
- مفتاح اختياري لإنشاء شحنة تلقائيًا عند تقديم الطلب (معطَّل افتراضيًا).

## الموجود في السورس:
- لا مفهوم شحنة أصلًا لتفعيل إنشائها التلقائي.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 154. تتبّع الشحنات (Shipment Tracking)

## المطلوب حسب المرجع:
- مزامنة مستمرة لحالة الشحنة (معلّق/معالجة/تم الاستلام/في الطريق/خارج للتوصيل/تم التسليم/مرتجع/ملغى/فشل التسليم) وتحديث الطلب تلقائيًا.

## الموجود في السورس:
- لا جدول/حالة شحن في أي schema.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 155. أنظمة المحاسبة (Accounting Systems)

## المطلوب حسب المرجع:
- Zoho Books, QuickBooks, Xero, Odoo. تصدير فواتير/عملاء/طلبات/منتجات، مزامنة ضرائب/مدفوعات، توليد قيود محاسبية.

## الموجود في السورس:
- لا يوجد أي كود متعلق بالمحاسبة أو الفواتير في كامل المشروع.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 156. تكاملات التقويم (Calendar Integrations)

## المطلوب حسب المرجع:
- Google Calendar, Outlook, Calendly. إنشاء/تحديث/إلغاء موعد، مزامنة التوفر، مزامنة التذكير، مزامنة ثنائية الاتجاه.

## الموجود في السورس:
- لا يوجد أي كود متعلق بمزامنة تقويم خارجي (المواعيد داخلية فقط).

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 157. خدمات البريد الإلكتروني — تكامل (Email Services Integration)

## المطلوب حسب المرجع:
- SMTP, Microsoft 365, Google Workspace, Resend، لكل Workspace بريده الخاص القابل للاتصال.

## الموجود في السورس:
- `src/lib/email/index.ts`: Resend أو Console Mock فقط، مُرسِل واحد على مستوى المنصة كلها (متغيّر بيئة)، وليس اتصالًا لكل Workspace.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا SMTP، لا Microsoft 365، لا Google Workspace، ولا إمكانية لكل Workspace ربط بريده الخاص.

---

# 158. خدمات الرسائل النصية SMS (SMS Services)

## المطلوب حسب المرجع:
- Twilio أو مزوّدين محليين. OTP، تذكير مواعيد، إشعارات طلبات، تحديثات حالة.

## الموجود في السورس:
- لا يوجد أي كود متعلق بإرسال SMS في كامل المشروع (OTP عبر البريد فقط).

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 159. إدارة الاتصال — التكاملات (Connection Management)

## المطلوب حسب المرجع:
- اتصال، قطع اتصال، إعادة اتصال، عرض الحالة، اختبار الاتصال، مزامنة الآن، عرض آخر مزامنة، عرض السجلات.

## الموجود في السورس:
- إنشاء/إلغاء/تفعيل مفتاح API أو اشتراك webhook فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- إعادة اتصال، عرض حالة صحة، اختبار اتصال، مزامنة الآن، عرض آخر مزامنة، عرض سجلات التسليم — لا شيء منها موجود.

---

# 160. إدارة بيانات الاعتماد (Credential Management)

## المطلوب حسب المرجع:
- بيانات اعتماد مُشفَّرة، لا تُعرَض أبدًا، دعم دوران آمن، مزوّدو OAuth مستقبليون.

## الموجود في السورس:
- مفاتيح API مُجزَّأة (SHA-256) بشكل صحيح، لكن سر توقيع الـ webhook (`secret`) **مخزَّن نصًا صريحًا** غير مشفَّر.

## النتيجة:
❌ غير مكتملة.

النواقص:
- سر الـ webhook غير مشفَّر عند التخزين، لا دعم "تدوير" (rotate) للسر (فقط إلغاء وإنشاء جديد)، لا بنية OAuth لمزوّدين خارجيين مستقبليين.

---

# 161. محرك المزامنة (Sync Engine)

## المطلوب حسب المرجع:
- مزامنة يدوية/تلقائية/مجدولة/عبر webhook، إعادة محاولة الفشل، كشف/حل التعارض، سجل مزامنة.

## الموجود في السورس:
- لا يوجد أي مجدول مزامنة أو جدول سجل مزامنة في كامل المشروع.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 162. محرك الـ Webhooks — التكاملات (Webhook Engine)

## المطلوب حسب المرجع:
- Webhooks واردة: تحقق توقيع، منع إعادة تشغيل الهجوم (replay)، إعادة محاولة الفشل، Dead Letter Queue، تسجيل.

## الموجود في السورس:
- `webhook-signature.ts`, `safe-webhook-fetch.ts`: توقيع HMAC للـ webhooks **الصادرة** فقط (من المنصة للخارج)، بحماية SSRF جيدة.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا نقطة استقبال webhook واردة من أي نوع (لا تحقق توقيع وارد، لا منع إعادة تشغيل) — المحرك الموجود اتجاه واحد فقط (صادر)، بلا إعادة محاولة أو Dead Letter Queue أو سجل تسليم دائم.

---

# 163. نظام الأحداث — التكاملات (Integrations Event System)

## المطلوب حسب المرجع:
- أحداث داخلية: طلب أُنشئ/حُدِّث، شحنة أُنشئت/سُلِّمت، موعد حُجِز، lead أُنشئ، عميل أُنشئ، فاتورة أُنشئت — كل تكامل يشترك في أحداثه فقط.

## الموجود في السورس:
- 11 نوع حدث مدعوم فعليًا (مطابق لأحداث الأتمتة نفسها): `lead_stage_changed, order_status_changed, conversation_handed_over, order_created, lead_created, appointment_created, appointment_status_changed, tag_added, message_received, message_replied, ai_failed`.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا أحداث شحنة (لا شحن أصلًا)، لا حدث "عميل أُنشئ" مستقل، لا حدث "فاتورة أُنشئت" (لا فوترة أصلًا).

---

# 164. المعالجة الخلفية — التكاملات (Background Processing)

## المطلوب حسب المرجع:
- كل التكاملات تعمل عبر عمّال خلفيين (Workers)، لا تحجب طلب المستخدم أبدًا.

## الموجود في السورس:
- إشعار مشتركي الـ webhook يعمل بشكل متزامن (`Promise.all`) داخل نفس مسار `dispatch()` مباشرة، وليس عبر عامل خلفي.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا عمّال خلفيين حقيقيين، لا ضمان بعدم حجب الطلب (نفس نمط النقص في محرك الأتمتة نفسه — #64).

---

# 165. مراقبة التكاملات (Integrations Monitoring)

## المطلوب حسب المرجع:
- صحة الاتصال، حالة المزامنة، المزامنات الفاشلة، أخطاء API، فشل webhook، الكمون، الاستخدام، آخر مزامنة ناجحة.

## الموجود في السورس:
- لا لوحة/جدول مراقبة من أي نوع؛ الفشل يُسجَّل فقط عبر `console.error`.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل الثمانية المذكورة — غير موجودة.

---

# 166. معالجة أخطاء التكاملات (Integrations Error Handling)

## المطلوب حسب المرجع:
- رسائل خطأ واضحة تشرح: ماذا حدث، لماذا، كيف يُصلَح. لا كشف استجابات المزوّد الخام.

## الموجود في السورس:
- رسائل `AppError` واضحة لأخطاء التحقق من مفاتيح API/اشتراكات webhook فقط (مثال: "Webhook URL must use https://").

## النتيجة:
❌ غير مكتملة.

النواقص:
- بما أنه لا يوجد أي مزوّد خارجي حقيقي مُتَّصل به (لا شحن/محاسبة/تقويم)، فلا وجود لترجمة أخطاء مزوّدين حقيقيين — الموجود فقط رسائل تحقق مدخلات المنصة نفسها.

---

# 167. إشعارات التكاملات (Integrations Notifications)

## المطلوب حسب المرجع:
- تكامل مُتَّصل/مقطوع، شحنة أُنشئت/فشلت/سُلِّمت، مزامنة اكتملت/فشلت، بيانات اعتماد منتهية، فشل webhook.

## الموجود في السورس:
- لا إشعارات خاصة بالتكاملات في نظام الإشعارات العام.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل الأنواع الثمانية المذكورة — غير موجودة.

---

# 168. أمان التكاملات (Integrations Security)

## المطلوب حسب المرجع:
- تشفير بيانات الاعتماد، تحقق كل طلب وارد، تسجيل تدقيق لكل إجراء تكامل، عزل المستأجرين، عدم كشف أسرار المزوّد.

## الموجود في السورس:
- مفاتيح API مُجزَّأة، عزل Workspace موجود فعليًا في كل مستودع، حماية SSRF قوية.

## النتيجة:
❌ غير مكتملة.

النواقص:
- سر الـ webhook غير مُشفَّر عند التخزين (نفس نقص #160)، لا سجل تدقيق مخصص لإنشاء/إلغاء مفاتيح API أو اشتراكات webhook.

---

# 169. توقّع مخاطر فقدان العملاء (Churn Risk Prediction)

## المطلوب حسب المرجع:
- تحليل تكرار التفاعل/سجل الطلبات/اتجاه المشاعر، تسجيل Low/Medium/High، تشغيل تنبيه/سير عمل تلقائي لعملاء VIP معرَّضين للخطر.

## الموجود في السورس:
- `src/features/campaigns/lib/churn-risk.ts`: نتيجة محسوبة من أيام منذ آخر طلب/آخر تواصل فقط (مقيَّدة رياضيًا بلا NaN)، معروضة كقائمة في صفحة Campaigns.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا اعتماد على تكرار التفاعل الفعلي أو تحليل المشاعر — فقط حداثة الطلب/التواصل. لا تشغيل تلقائي لأي تنبيه أو سير عمل عند دخول عميل VIP بمنطقة خطر عالية (القائمة عرض سلبي فقط).

---

# 170. تسجيل نقاط العملاء المحتملين المتقدم (Advanced Lead Scoring & Purchase Intent)

## المطلوب حسب المرجع:
- نقاط 0-100 ديناميكية من سياق المحادثة الفعلي/الميزانية/سرعة التفاعل، توقّع توقيت/احتمال الشراء لترتيب أولوية طابور المبيعات.

## الموجود في السورس:
- نفس `lead-score.ts` الحتمي المذكور في #41 — بلا تحليل محتوى محادثة فعلي.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا توقّع لتوقيت/احتمال الشراء، لا ترتيب أولوية طابور مبيعات مبني على هذا التوقّع.

---

# 171. تحديد شرائح CRM للحملات (CRM Segment Definition)

## المطلوب حسب المرجع:
- بناء جمهور حملة من صفات CRM (مثال: خامل > 30 يومًا، مخاطر فقدان عالية، VIP).

## الموجود في السورس:
- `campaign.service.ts`: فلترة بمرحلة دورة الحياة أو وسم واحد فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا معيار "خامل > 30 يومًا" أو "مخاطر فقدان عالية" كخيار تصنيف مباشر داخل باني الحملة (قائمة churn-risk منفصلة تمامًا وغير مرتبطة باستهداف الحملات).

---

# 172. باني الحملات والقوالب (Campaign Builder & Templates)

## المطلوب حسب المرجع:
- بناء حملة، اختيار قالب رسالة.

## الموجود في السورس:
- `campaign-manager.tsx`: حقلا موضوع/رسالة نصية حرة لكل حملة، بلا مكتبة قوالب.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا قوالب رسائل قابلة لإعادة الاستخدام إطلاقًا.

---

# 173. محرك الامتثال وتحديد المعدل (Compliance & Rate-Limit Engine)

## المطلوب حسب المرجع:
- حماية من السبام وإنفاذ سياسة القناة قبل الإرسال.

## الموجود في السورس:
- لا فحص تقييد معدل أو امتثال داخل `sendCampaign` — جدول `rate-limits.ts` موجود بالنظام لكن غير مُستخدَم هنا إطلاقًا.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة عن هذه الوحدة تحديدًا.

---

# 174. إرسال الحملات (Campaign Dispatch)

## المطلوب حسب المرجع:
- إرسال عبر طابور غير متزامن، من قناة/حساب Workspace المتصل الخاص (WhatsApp/Instagram).

## الموجود في السورس:
- حلقة إرسال متزامنة (for-loop) داخل نفس الطلب، عبر البريد الإلكتروني (Resend) — وليس عبر WhatsApp/Instagram (موثَّق كقرار مؤجَّل بانتظار Meta OAuth).

## النتيجة:
❌ غير مكتملة.

النواقص:
- ليس عبر طابور غير متزامن، ليس عبر القناة المطلوبة في المرجع (WhatsApp/Instagram) — بديل بريد إلكتروني مؤقت.

---

# 175. أتمتة ومحفزات الحملات (Campaign Automation & Triggers)

## المطلوب حسب المرجع:
- إرسال فوري أو مجدول، متغيّرات قالب ديناميكية، تفريع متابعة تلقائي حسب رد العميل.

## الموجود في السورس:
- إرسال فوري فقط (لا حقل جدولة في schema).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا جدولة، لا متغيّرات ديناميكية (الرسالة نص ثابت)، لا تفريع متابعة تلقائي.

---

# 176. أمان وامتثال الحملات (Campaign Security & Compliance)

## المطلوب حسب المرجع:
- تحقق موافقة مسبقة (opt-in)، حالات موافقة القالب، تحديد معدل مانع للحظر، عزل صارم للمستأجرين، تسجيل تدقيق شامل.

## الموجود في السورس:
- عزل Workspace موجود فعليًا وصحيح، فحص صلاحية `campaigns.manage` فعلي.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا تحقق موافقة مسبقة (opt-in/opt-out)، لا حالات موافقة قالب، لا تحديد معدل، لا تسجيل تدقيق للحملات الجماعية أو تغييرات نقاط التنبؤ.

---

# 177. بنية وحدة الإعلانات (Ads Module Architecture)

## المطلوب حسب المرجع:
- Workspace Settings -> Meta OAuth -> مدير حسابات إعلانية -> محرك مزامنة Campaign/Ad Set/Ad -> معالجة المقاييس -> محرك إسناد -> إنشاء lead بـ CRM -> رؤى AI.

## الموجود في السورس:
- `ads.ts`, `ads.service.ts`: جدولا `ad_accounts`/`ad_campaigns` فقط، بلا OAuth، بلا مزامنة، محرك إسناد موجود لكن مبسّط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- Meta OAuth، مدير حسابات فعلي (زر معطَّل فقط)، محرك مزامنة Ad Set/Ad، معالجة مقاييس، رؤى AI — كل هذا غائب (محجوب خارجيًا بانتظار حساب Meta Developer، موثَّق).

---

# 178. تكامل Meta Marketing API

## المطلوب حسب المرجع:
- Facebook Ads, Instagram Ads, Meta Business Manager, Pages, Instagram Professional Accounts, Meta Lead Forms.

## الموجود في السورس:
- لا استدعاء API حقيقي لـ Meta في أي مكان — فقط تسمية "meta" كقيمة enum نصية.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة، محجوبة خارجيًا).

النواقص:
- كل التكاملات الستة المذكورة — غير موجودة.

---

# 179. تدفق الاتصال الأولي بـ Meta (Initial Connection Flow)

## المطلوب حسب المرجع:
- Meta OAuth 2.0 -> تحقق صلاحيات -> تبادل رمز -> اكتشاف الحسابات/الصفحات/حسابات Instagram/نماذج Lead -> تخزين بيانات اعتماد مشفَّرة.

## الموجود في السورس:
- زر "Coming soon" معطَّل فقط، بلا أي كود OAuth.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة، محجوبة خارجيًا).

النواقص:
- كل خطوات التدفق الخمس — غير موجودة.

---

# 180. محرك المزامنة الخلفية للإعلانات (Background Synchronization Engine)

## المطلوب حسب المرجع:
- عامل غير متزامن يزامن الحسابات/الحملات/Ad Sets/الإعلانات/التصاميم/مقاييس الأداء.

## الموجود في السورس:
- لا يوجد أي عامل/جدولة مزامنة من أي نوع.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة، محجوبة خارجيًا).

النواقص:
- الميزة بالكامل غائبة.

---

# 181. بنية بيانات Meta (Meta Structure / DB Schema)

## المطلوب حسب المرجع:
- التسلسل الهرمي: Ad Account -> Campaign -> Ad Set -> Ad -> Creative، بحقول تفصيلية لكل مستوى.

## الموجود في السورس:
- جدولان فقط: `ad_accounts`, `ad_campaigns` — بلا Ad Set، بلا Ad، بلا Creative.

## النتيجة:
❌ غير مكتملة.

النواقص:
- مستويا Ad Set وAd والـ Creative بالكامل — غير موجودين كجداول.

---

# 182. محرك الإسناد (Lead Attribution Engine)

## المطلوب حسب المرجع:
- كل lead يعرف مصدره الكامل: campaignId, adSetId, adId, source, UTM, leadId, customerId, orderId, revenueGenerated.

## الموجود في السورس:
- `ad-campaign.repository.ts` (`getAttributionStats`): إسناد على مستوى الحملة فقط، مطابقة نصية بين `contacts.source` و`utmCampaign`، الإيراد مُجمَّع حيًّا (live join) من الطلبات المُسلَّمة/المكتملة — تم التحقق أن الحساب صحيح رياضيًا (لا ازدواج عدّ).

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا إسناد على مستوى Ad Set أو Ad (حملة فقط)، لا حفظ دائم لكل إسناد فردي (leadId/customerId/orderId/revenueGenerated) — الإيراد يُحسَب حيًّا وليس مخزَّنًا لكل سجل.

---

# 183. نظام تتبع UTM (UTM Tracking System)

## المطلوب حسب المرجع:
- توليد/تتبّع تلقائي: utm_source, utm_medium, utm_campaign, utm_content — على كل رابط.

## الموجود في السورس:
- حقل `utmCampaign` وحيد فقط، يُكتَب يدويًا من طرف المستخدم.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا utm_source، لا utm_medium، لا utm_content، لا توليد تلقائي لروابط متتبَّعة.

---

# 184. تكامل نماذج Meta Lead (Meta Lead Form Integration)

## المطلوب حسب المرجع:
- نموذج Facebook Instant Form -> webhook -> إنشاء lead -> CRM -> محفّز أتمتة -> متابعة AI.

## الموجود في السورس:
- لا يوجد أي كود متعلق بنماذج Meta Lead في كامل المشروع.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة، محجوبة خارجيًا).

النواقص:
- الميزة بالكامل غائبة.

---

# 185. تكامل الإعلانات مع CRM (Ads-CRM Integration)

## المطلوب حسب المرجع:
- كل تفاعل إعلاني ينشئ/يحدّث Contact/Lead/Conversation/Deal تلقائيًا، مع تسجيل مصدر الـ lead (اسم الحملة/الإعلان).

## الموجود في السورس:
- الإسناد يربط فقط بجهات اتصال موجودة مسبقًا عبر حقل `source` نصي حر — لا إنشاء تلقائي لجهة اتصال من تفاعل إعلاني حقيقي.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا إنشاء/تحديث تلقائي حقيقي لـ Contact/Lead من تفاعل إعلان — يعتمد على أن جهة الاتصال أُنشئت مسبقًا بطريقة أخرى (نموذج الموقع، API عام، يدويًا) وأن حقل `source` يطابق يدويًا.

---

# 186. تحليلات أداء الإعلانات (Ad Performance Analytics)

## المطلوب حسب المرجع:
- الإنفاق، الظهور، النقرات، CTR، CPL، leads، الطلبات، الإيراد، ROAS.

## الموجود في السورس:
- `attribution-report.tsx`: عدد جهات الاتصال + الإيراد فقط.

## النتيجة:
❌ غير مكتملة.

النواقص:
- الإنفاق، الظهور، النقرات، CTR، CPL، leads (كعدد منفصل عن جهات الاتصال)، ROAS — كل هذه المقاييس السبعة غير موجودة.

---

# 187. محرك ذكاء الإعلانات بالـ AI (AI Ads Intelligence Engine)

## المطلوب حسب المرجع:
- رؤى مثل: "الحملة A تُنتج عملاء بتكلفة أقل 40%"، "هذا التصميم بتفاعل عالٍ وتحويل منخفض".

## الموجود في السورس:
- لا يوجد أي كود لتوليد رؤى AI خاصة بالإعلانات.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 188. توصيات التحسين الآلية (Automated Optimization Recommendations)

## المطلوب حسب المرجع:
- توصية بزيادة الميزانية، إيقاف حملة ضعيفة، تغيير الاستهداف، اختبار تصميم جديد، إنشاء سير عمل متابعة — بلا تغيير تلقائي دون موافقة المستخدم.

## الموجود في السورس:
- لا يوجد أي كود لتوليد توصيات تحسين للإعلانات.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- الميزة بالكامل غائبة.

---

# 189. واجهة الإعلانات (Ads UI)

## المطلوب حسب المرجع:
- الشريط الجانبي "Marketing > Ads"، صفحات منفصلة: نظرة عامة، الحملات، الإسناد، رؤى AI.

## الموجود في السورس:
- صفحة واحدة مدمجة `/dashboard/ads` تحت قسم "GROWTH" في الشريط الجانبي (وليس "Marketing").

## النتيجة:
❌ غير مكتملة.

النواقص:
- تصنيف الشريط الجانبي مختلف عن المرجع، لا صفحات منفصلة للأقسام الأربعة (كلها أقسام داخل صفحة واحدة)، لا صفحة "رؤى AI" مستقلة إطلاقًا.

---

# 190. أمان وصلاحيات الإعلانات (Ads Security & Permissions)

## المطلوب حسب المرجع:
- فقط Owner/Admin/Manager يمكنهم الاتصال بـ Meta/عرض بيانات الإعلانات/إدارة الحملات. بيانات اعتماد مشفَّرة وقابلة للتدوير ومُدقَّقة.

## الموجود في السورس:
- صلاحية `ads.manage` مُسنَدة فعليًا لـ Owner/Admin/Manager فقط (وليس Agent)، عزل Workspace صحيح في كل مستودع.

## النتيجة:
❌ غير مكتملة.

النواقص:
- لا بيانات اعتماد مشفَّرة/قابلة للتدوير موجودة أصلًا لتدقيقها (لا اتصال Meta حقيقي بعد).

---

# 191. سجل تدقيق الإعلانات (Ads Audit Logging)

## المطلوب حسب المرجع:
- تسجيل: اتصال Meta، تجديد الرمز، مزامنة الحملة، تحديثات الإسناد، إنشاء lead من إعلان، توصيات AI.

## الموجود في السورس:
- لا يوجد أي استدعاء تسجيل تدقيق في كامل وحدة `src/features/ads`.

## النتيجة:
❌ غير مكتملة (فعليًا غير موجودة).

النواقص:
- كل الأنواع الستة المذكورة — غير موجودة.

---

# الخلاصة النهائية

## الميزات المكتملة 100% (✅) — 13 ميزة من أصل 191
1. تعدد المستأجرين (Multi-Tenant) — #4
2. وكيل الذكاء الاصطناعي (بنية MVP) — #22
3. إعدادات وكيل الذكاء الاصطناعي — #23
4. محرك الأفعال / استدعاء الأدوات (Tool Calling) — #31
5. إنشاء العملاء المحتملين تلقائيًا (Leads) — #40
6. أتمتة خط الأنابيب (حدث lead_stage_changed) — #43
7. الوسوم (Tags) — #51
8. مولد سير العمل بالذكاء الاصطناعي — #62
9. الصلاحيات على الأتمتة (RBAC للـ workflows) — #67
10. رؤى الذكاء الاصطناعي (AI Insights بلوحة التحكم) — #73
11. خريطة التنقل بـ 5 أقسام (Part 13B) — #119
12. الإخفاء عن المستأجرين (No AI Terminology + عزل Super Admin) — #123
13. كتالوج المنتجات على الموقع (بلا ازدواجية بيانات) — #130

## الميزات غير المكتملة جزئيًا (❌ لكن يوجد أساس حقيقي) — الغالبية الساحقة
كل الميزات المرقّمة من 1 إلى 191 باستثناء المذكورة أعلاه هي "غير مكتملة" بدرجات متفاوتة — من نقص حقل واحد (مثال: #46 الخدمات ينقصها فقط حقل "المدة") إلى نقص جوهري شبه كامل (مثال: #47 الطلبات بلا خصم/ضريبة/توصيل/دفع). راجع كل قسم أعلاه للتفاصيل الدقيقة.

## ميزات مطلوبة في المرجع وغير موجودة إطلاقًا في السورس (فراغ كامل، ليس نقصًا جزئيًا)
- تخزين الملفات الموحّد (#6)
- اتصال القنوات الفعلي / Meta OAuث (#9)
- نظام Webhooks للقنوات (#11)
- محرك المزامنة العام (Sync Engine) للتكاملات (#161)
- بنية Connector/Adapter للتكاملات (#149)
- تكاملات الشحن بالكامل (#151-154)
- أنظمة المحاسبة (#155)
- تكاملات التقويم (#156)
- خدمات SMS (#158)
- الفواتير (Invoices) بكل أنواعها (#94)
- المدفوعات الحقيقية وتدفق الدفع (#92-93)
- الخصومات/الكوبونات (#99)
- المبالغ المستردة (#100)
- بنية الضرائب (#101)
- إدارة الخطة الذاتية من طرف العميل (#96)
- صحة النظام (System Health) للمشرف الأعلى (#114)
- مراقبة الأخطاء (Error Monitoring / Sentry) (#115)
- إشعارات المشرف الأعلى (#117)
- قوالب سير العمل الجاهزة (#65)
- معالج إعداد الموقع (Website Setup Wizard) (#125)
- القوالب (Themes) للموقع (#127)
- باني الأقسام (Section Builder) (#129)
- صفحة المنتج المستقلة (#132)
- مساعد الموقع بالذكاء الاصطناعي (#136)
- التوصيات الذكية على الموقع (#137)
- العروض الترويجية (#138)
- باني النوافذ المنبثقة (#139)
- روابط التواصل الاجتماعي على الموقع (#141)
- Meta Pixel والتتبع (#142)
- SEO للموقع (#143)
- التقييمات والشهادات (#144)
- المدونة (#145)
- تحليلات الموقع (#146)
- تحليلات العملاء (Customer Analytics: CLV/Retention/Churn في لوحة التحليلات) (#81)
- الأدوات الحية/التحديث اللحظي في أي مكان بالنظام (#21, #72)
- تكامل Meta Marketing API الفعلي بكل أجزائه (#178-184، #187-188، #191) — محجوب خارجيًا بانتظار حساب Meta Developer.
